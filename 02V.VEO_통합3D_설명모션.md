# VEO 통합형 장면 맞춤 3D 설명모션 지침

## 목적

고대 유물·건축·지하 공간을 설명할 때 그래픽을 영상 위에 붙인 HUD처럼 만들지 않는다.
설명 그래픽도 카메라가 촬영하는 3D 세계 안에 존재하게 Veo/Flow 생성 프롬프트에 직접
포함한다. 핵심은 **화살표를 넣는 것**이 아니라 **장면의 질문을 가장 잘 설명하는 입체
시각 언어를 고르는 것**이다.

기본 순서는 **장면 질문 → 증거 범위 → 그래픽 기능 → 재질·색 → 월드 앵커 → 시간
동작 → Veo 생성 → 키프레임 QA**다. Remotion 후합성은 배포 경로에 넣지 않는다.

---

## 1. 모션 소유권

| 소유권 | 용도 | 예시 |
|---|---|---|
| `GENERATED_PHYSICS` | 실제 물리 현상 | 흙, 먼지, 물, 연기, 천, 도구 충격 |
| `VEO_INTEGRATED_3D` | 관객 이해를 위한 월드 고정형 3D 설명 그래픽 | 경로, 유선, 물질 흐름, 힘, 치수, 스캔, 단면, 분해 순서 |
| `INFO_OVERLAY` | 정확성이 필요한 최소 문자 | 숫자, 지명, 증거 상태 배지 |
| `NONE` | 설명 모션 없음 | 인물 표정, 유물 정물 관찰 |

한 장면에서 `GENERATED_PHYSICS + VEO_INTEGRATED_3D`는 함께 쓸 수 있다. 다만 같은
현상을 실제 먼지와 그래픽으로 두 번 과장하지 않는다. 한 프레임의 주인공 그래픽은 한
종류만 쓴다.

---

## 2. 생성 전에 잠글 12개 항목

`VEO_INTEGRATED_3D` 장면은 프롬프트부터 쓰지 않는다. 먼저 아래를 장면표에 기록한다.

1. **설명 질문** — 관객이 이 장면에서 무엇을 이해해야 하는가
2. **증거 관계** — 발굴확인·측정확인·문헌기록·학술해석·미확인 중 무엇을 시각화하는가
3. **고정 구조** — 영상 내내 형태·개수·위치가 바뀌면 안 되는 유물·벽·기둥·통로
4. **그래픽 기능** — 아래 허용 기능 중 하나
5. **시각 언어** — 형태·재질·색·투명도·광량. 장면의 물질과 의미에 맞게 결정
6. **시작 앵커** — 화면 좌표가 아닌 실제 물체·표면의 명칭
7. **경유 앵커** — 굴곡·분기·높이 변화가 일어나는 실제 구조 1~4개
8. **도착 앵커** — 그래픽이 끝나는 실제 물체·개구부·경계
9. **가림 순서** — 무엇의 앞→뒤→앞으로 지나가는지
10. **카메라 관계** — 카메라가 이동해도 같은 월드 위치에 남는 방식
11. **시간 단계** — 발생·이동·도착 반응·퇴장의 초 단위 구간
12. **도착 반응** — 먼지, 반사광, 진동, 물질 변화, 초점 이동 중 하나

이 중 하나라도 알 수 없으면 3D 그래픽을 넣지 않는다. 먼저 고증 자료, 시작 이미지,
장면 구도를 다시 설계한다.

장면표의 `veo_graphic`에는 최소한 다음 열 개 필드를 넣는다.

```json
{
  "function": "ROUTE_PATH",
  "evidence_relation": "측정확인된 통로의 연결만 표시",
  "visual_language": "warm ivory stone-dust route ribbon, matte, 35% transparent",
  "start": "cracked lower threshold",
  "via": ["fallen slab", "rear square pillar"],
  "end": "upper circular vent",
  "occlusion": "behind the square pillar, then reappears",
  "timing": "0.6s emerge, 1.0-5.8s travel, 6.1s arrive, 7.2s fade",
  "camera_relation": "fixed to corridor geometry during forward dolly",
  "arrival_reaction": "one small dust lift at the vent"
}
```

---

## 3. 장면 맞춤 기능·재질·색

빨강은 기본색이 아니다. 색은 장식이 아니라 의미와 물성을 구분하는 신호다. 장면의 실제
재료와 충분히 대비되면서도 다큐멘터리 톤을 해치지 않는 한 가지 주인공 색을 고른다.

| 기능 | 권장 시각 언어 | 사용 장면 |
|---|---|---|
| `ROUTE_PATH` | 석분 같은 아이보리·황토 리본과 작은 방향 펄스 | 이동 경로, 환기 통로, 진입 순서 |
| `AIRFLOW_STREAM` | 옅은 온백색·황토색 반투명 유선, 미세 먼지 반응 | 바람길, 환기, 연기 이동 |
| `MATERIAL_FLOW` | 대상 물질의 실제 재질·굴절·점도 | 물, 모래, 수은, 배수, 퇴적 |
| `FORCE_PATH` | 청동·구리·호박색 에너지 결, 접촉면을 잇는 짧은 펄스 | 장력, 하중, 작동 기구 |
| `DIMENSION_LINE` | 따뜻한 회백색 또는 배경 대비 중성색의 얇은 입체 틱·선 | 높이, 폭, 깊이, 간격 |
| `SCAN_WAVE` | 절제된 청록·청백색 측정파 | 스캔, 지구물리 탐사, 표면 판독 |
| `SECTION_REVEAL` | 토층·석재의 실제 절단면 재질과 미세 발광 경계 | 지층 단면, 봉토 내부 층위 |
| `EXPLODED_SEQUENCE` | 원재료 색을 유지한 부품 분리와 순번 이동 | 결구, 석판, 기계 부품의 조립 관계 |
| `DANGER_ZONE` | 제한된 주홍·진사색 경계 또는 위험 경로 | 침입, 붕괴, 금지·위험 구역 |

- 수은은 은빛 거울광과 무거운 액체성으로 보여 준다. 근거 없는 독성 증기나 네온광을
  추가하지 않는다.
- 바람은 빨간 화살표가 아니라 공간을 휘감는 옅은 유선과 먼지·천의 반응으로 보여 준다.
- 치수는 물체의 두 실제 모서리에서 틱이 자라야 한다. 숫자는 Veo가 아니라 정확한 자막이
  전달한다.
- 위험·침입 장면이 아닌데 습관적으로 빨강을 쓰면 불합격이다.
- 한 장면 안에서 기능을 여러 색으로 무지개처럼 바꾸지 않는다.

---

## 4. 공통 프롬프트 골격

```text
Single continuous [duration]-second shot in the unchanged [historical space].
The camera [one precise camera move]. At [time], one restrained [material, colour,
transparency] 3D [graphic form] emerges from [physical start anchor]. It follows
[real surface or volume], passes [via anchors in order], is fully occluded by
[foreground structure], reappears with correct parallax, and ends at [physical end
anchor]. The graphic remains anchored in the physical world, shares the scene's
perspective, depth, lighting, and surface reflections, and never attaches to the
screen. On arrival, [one physical reaction], then the graphic fades before the end.
No text, no numbers, no labels, no floating HUD, no screen-space graphics, no
unverified internal structure, no geometry changes, no extra graphics, no cut.
```

### 위험·침입 경로에서만 쓰는 주홍색 예시

```text
one restrained cinnabar-red semi-transparent 3D danger route ribbon,
with four small repeated directional arrowheads, anchored in the physical world
```

이 예시는 색상 표준이 아니라 `DANGER_ZONE`의 한 사례다. 일반 통로 설명에는 주변 석재와
구분되는 아이보리·황토 재질을 우선한다.

### 바람길 예시

```text
three thin warm-white translucent airflow ribbons curve through the chamber volume;
tiny dust particles and the edge of one hanging cloth respond a fraction later
```

### 수은 흐름 예시

```text
a narrow mirror-silver mercury-like material flow with heavy cohesive surface
tension follows only the documented channel geometry, reflecting nearby stone
```

---

## 5. 기능별 필수 동작

### `ROUTE_PATH` / `DANGER_ZONE`

얇은 리본이나 관형 경로 위에 작은 방향 펄스 3~5개를 이동시킨다. 구조물과 같은 원근을
따르고, 기둥 뒤에서 가려졌다가 다시 나타난다. 벽을 뚫거나 화면에 붙으면 실패다.

### `AIRFLOW_STREAM`

공기 자체보다 공간의 부피와 반응을 보여 준다. 유선은 모서리를 따라 휘고 먼지·연기·천이
조금 늦게 반응해야 한다. 폭발형 입자나 에너지 광선은 금지한다.

### `MATERIAL_FLOW`

물질의 실제 점도·중력·반사·굴절을 유지한다. 출발 저장부, 경유 수로, 도착부를 고정하고
확인되지 않은 배관이나 방을 새로 만들지 않는다.

### `FORCE_PATH`

힘이 전달되는 실제 접촉면을 순서대로 지정한다. `string → catch → pivot → trigger`처럼
부품과 접촉 순서를 잠근다. 새 부품이나 폭발형 에너지 효과는 금지한다.

### `DIMENSION_LINE`

두 실제 모서리에서 얇은 3D 끝점 틱이 자라고 그 사이가 연결된다. 선은 카메라가 움직여도
같은 측정 평면에 남는다. 읽을 숫자는 생성하지 않는다.

### `SCAN_WAVE`

벽·지표·유물 표면을 따라 움직이는 얇은 측정파로 쓴다. 스캔 결과로 확인되지 않은 내부
형상을 새로 드러내지 않는다.

### `SECTION_REVEAL`

절단 평면이 흙과 구조를 실제 깊이 순서대로 통과하며 단면을 드러낸다. 확인된 층위만
표시하고, 미확인 중앙 묘실은 불투명한 미지 영역으로 남긴다.

### `EXPLODED_SEQUENCE`

부품은 실제 결합 축을 따라 짧게 분리되고 같은 축으로 되돌아온다. 원래 개수·크기·재질을
바꾸지 않는다.

---

## 6. 길이별 복잡도 상한

| 길이 | 허용 복잡도 |
|---:|---|
| 4초 | 그래픽 1개, 시작→도착, 경유 최대 1개, 안정된 카메라 |
| 6초 | 그래픽 1개, 경유 2개, 가림 1회, 느린 돌리·패닝 |
| 8초 | 경유 3~4개, 가림 1~2회, 도착 반응, 카메라 시차 |
| 10초 | Omni Flash 수정 또는 복잡한 절개 이동. 한 장면 한 질문은 유지 |

복잡도를 더 넣고 싶으면 프롬프트를 길게 하지 말고 장면을 나눈다.

---

## 7. 생성 순서

### 1차 — 단일 Veo I2V

그래픽이 붙을 구조가 전부 보이는 시작 이미지를 사용한다. 시작 이미지에는 글자와
설명선을 넣지 않는다. 영상 프롬프트에서 시간 순서대로 그래픽을 발생시킨다.

### 2차 — Gemini Omni Flash 수정

구조·카메라는 잘 나왔지만 설명 그래픽만 실패했다면 베이스 영상을 버리지 않는다. 최대
10초 구간을 Omni Flash로 편집하며 아래처럼 그래픽만 수정한다.

```text
Keep every artifact, stone, opening, camera move, lighting change, duration, and
frame timing unchanged. Add only one [scene-specific material and colour] 3D
[graphic function] following [start → via → end]. Preserve all occlusion and parallax.
```

같은 수정에서 카메라·색보정·구조·그래픽을 동시에 바꾸지 않는다.

---

## 8. 불합격 기준

- 그래픽이 화면 HUD처럼 카메라와 함께 붙어서 이동한다.
- 구조물 앞에서만 떠 있고 뒤로 가려지지 않는다.
- 시작·경유·도착 앵커 중 하나를 건너뛴다.
- 경로·유선·물질이 벽을 뚫거나 중력을 무시한다.
- 장면의 의미와 무관한 빨강·네온·다색 그래픽이 나온다.
- 같은 기능의 그래픽이 복제되거나 중간에 재질·색이 바뀐다.
- 구조물·부품·개구부의 개수나 형태가 바뀐다.
- 읽을 수 없는 문자·숫자·라벨이 생긴다.
- 카메라가 지정 방향과 다르게 움직인다.
- 끝에서 그래픽이 갑자기 잘린다.

첫·중간·가림·도착·끝의 최소 5개 프레임을 뽑아 전부 통과해야 승인한다.

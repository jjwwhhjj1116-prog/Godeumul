# 00. 함정 목록 — 같은 실수를 두 번 하지 않기 위한 표

EP01(진시황릉)을 처음부터 끝까지 돌리면서 실제로 걸린 것들이다.
**전부 코드나 지침에 박아 두었다.** 이 표는 "왜 그렇게 되어 있는지"의 근거다.

새 회차를 시작하기 전에 이 표를 한 번 훑는다.
새로 걸린 게 생기면 **여기에 한 줄 추가하고, 반드시 코드나 지침에도 박는다.**
표에만 적고 끝내면 다음에 또 걸린다.

| 상태 | 뜻 |
|---|---|
| **코드** | 스크립트가 자동으로 막는다. 사람이 신경 쓸 필요 없다 |
| **지침** | 문서에 규칙으로 적혀 있다. 작업할 때 읽으면 안 걸린다 |
| **수동** | 자동화 불가. 체크리스트로만 관리한다 |

---

## 1단계 — 대본

| # | 증상 | 원인 | 규칙 | 어디에 |
|---|---|---|---|:--:|
| 1-1 | "훅이 너무 약하다" | 후킹에 필수 3요소가 빠졌다 | **훅 필수 3요소**: ① 대상을 즉시 명시 ② 모순을 한 문장으로 ③ 질문으로 닫기 | 지침 (01) |
| 1-2 | "문맥이 단절된다" | 문장이 각자 논다 | **문장 연결 3종 세트**: 앞 문장의 말을 받아서 시작 / 자문자답 / 「그런데」로 뒤집기 | 지침 (01) |
| 1-3 | "이야기 끌어가는 밀도가 약하다" | 출토품을 나열하고 다음 의문으로 연결하지 않았다 | **질문→증거→해석→더 큰 질문**을 한 묶음으로 쓴다. 각 답이 다음 질문을 열어야 한다 | 지침 (01) |
| 1-4 | 주제 목록을 안 보고 대본을 썼다 | 주제 번호만 듣고 추측했다 | 대본 전에 **`기획/주제100선.md` 의 해당 번호를 반드시 읽는다** | 지침 (01) |
| 1-5 | 지침의 모범 대본이 다음 회차까지 오염시켰다 | 진시황릉의 수은·모래·자동 쇠뇌를 검증 없이 모범 답안으로 고정했다 | 주제별 결론을 지침에 박지 않는다. 조사노트에서 `발굴확인/측정확인/문헌기록/학술해석/미확인`을 나눈 뒤 중심 질문을 정한다 | 지침 (01/01L) |
| 1-6 | 대본이 길어 영상이 3분을 넘을 뻔 | 발화속도를 추측했다 | **실측 9.35자/초.** 기획은 보수적으로 8.0자/초, 대본 상한 **1,400자** | 코드 |
| 1-7 | 모든 유물이 공학 문제와 해법 이야기로 꼬였다 | 신비한 건축사전의 `장애물→역발상→메커니즘` 구조를 채널 전체에 강제했다 | 공학은 실제 핵심 증거일 때만 쓴다. 기본 구조는 발견→출토품→역사 맥락→공간 반전→증거의 경계→미지다 | 지침 (01/01L) |
| 1-8 | 결말이 맥없이 “아직 모른다”로 끝났다 | 확인된 사실과 모르는 이유를 회수하지 않았다 | 결말에서 **확인된 것 / 역사적 의미 / 모르는 것 / 모르는 이유 / 필요한 다음 증거**를 회수한다 | 지침 (01/01L) |

## 2단계 — 장면 분할 · 프롬프트

| # | 증상 | 원인 | 규칙 | 어디에 |
|---|---|---|---|:--:|
| 2-1 | 공학과 무관한 장면까지 단면·분해도로 만들었다 | 해부 쿼터 45%와 L3→L4→L5를 강제했다 | 장면 유형별 최소 수를 없앤다. 단면·분해도·메커니즘은 공간·부품·작동을 이해시키는 경우에만 선택한다 | 지침 (02) |
| 2-2 | ★ "1, 2번이 왜 똑같이 뒤로 쭉 빠지냐" | 카메라를 장면의 행동이 아니라 고정 쿼터로 배정했다 | 카메라는 손·도구·유물·공간 관계를 따라간다. 같은 무빙 2연속은 피하되, 증거 관찰에 필요하면 정지도 허용한다 | 지침 (02) |
| 2-3 | 시각화 지침이 두 개라 어느 걸 따를지 모름 | v1 백업을 남겨 뒀다 | **지침은 항상 하나만 둔다.** 갱신은 덮어쓰기. 이전 판은 git 이력에 있다 | 지침 (02) |
| 2-4 | 한 컷에 발견·줌·단면 변환·지하 공개를 모두 넣었다 | 한 시작 이미지로 불가능한 구도 변화를 묶었다 | **한 장면 = 이미지 1 + 영상 프롬프트 1 + 연속 행동 1.** 표현 방식·증거 상태가 바뀌면 다음 장면으로 나눈다 | 지침 (02) |
| 2-5 | ★★ 중국 진나라인데 **유럽인 얼굴에 로마식 튜닉**이 나왔다 | 프롬프트가 `laborers in linen tunics` 처럼 문명 무국적 단어만 썼다. **지침의 LABOR 예시 자체가 그렇게 가르치고 있었다.** 이미지 모델의 기본값은 서양이다 | **고증 카드**(`02b.고증카드.md`)를 프롬프트보다 먼저 쓰고, [인물 앵커]를 매 컷 통째로 복사한다. tunic·toga 는 금지어 | 지침 (02) |
| 2-6 | ★★ 진시황릉 봉토가 **둥근 풀언덕**으로 나왔다 (실제는 사각 계단식 절두 피라미드) | v2.0 지침이 **"등록 유물의 형태 묘사는 금지"** 였다. 이름만으로는 모델이 유물을 모른다 | **규칙을 뒤집었다.** 형태 문장을 고증 카드에 한 번 확정하고 매 컷 글자 그대로 복사한다. 착각 포인트는 명시적으로 부정한다("NOT a round hill") | 지침 (02) |
| 2-7 | 건물이 일본 사찰·서양 성채처럼 나온다 | 건축 양식 단어가 없었다 | DIORAMA 규칙에 [건축 앵커] 필수화. 지붕·기둥·처마 양식을 명시 | 지침 (02) |
| 2-9 | ★ 지침에 적었는데도 또 틀린다 | 문서는 사람이 안 읽으면 그만이다. EP01은 지침이 있는데도 고증 앵커가 없었다 | **`prompt_check.py`가 FLOW 투입 전에 막는다.** 장면 번호·1:1 프롬프트·4/6/8/10초·TTS 길이·9:16·고증 앵커를 검사한다 | **코드** |
| 2-8 | 네거티브가 타 문명을 안 막는다 | `no people in the foreground` 뿐이었다 | 네거티브 꼬리에 유럽 얼굴·로마 복식·고전 기둥·중세 갑옷·시대착오 도구 차단 추가 | 지침 (02) |

## 3단계 — TTS

| # | 증상 | 원인 | 규칙 | 어디에 |
|---|---|---|---|:--:|
| 3-1 | 0바이트 mp3 가 생성됨 | API 오류 응답을 그대로 파일로 썼다 | **1KB 미만 응답은 거부**하고 본문을 찍는다 | **코드** |
| 3-2 | 모델이 `language_code` 를 거부 | 모델별로 지원이 다르다 | 거부되면 **language_code 없이 자동 재시도** | **코드** |
| 3-3 | 실수로 크레딧을 태울 뻔 | 실행하면 바로 호출됐다 | **기본이 드라이런.** `--run` 을 붙여야 실제 호출 | **코드** |
| 3-4 | 같은 문장을 두 번 결제 | 재실행 때 전부 다시 뽑았다 | 문장 **해시 캐시**. 안 바뀐 장면은 건너뛴다 | **코드** |
| 3-5 | 순서가 뒤집혔다 | 2단계를 먼저 다 끝내고 TTS 를 했다 | **2a → 3(TTS) → 2b.** 옴니 길이(4/6/8/10초)를 고르려면 실측이 먼저 필요하다 | 지침 (README) |
| 3-6 | ★ 나레이션이 하드 클리핑 | 투탕카멘 템플릿의 캡컷 볼륨 **1.778(+5dB)** 을 물려받았다. 캡컷엔 리미터가 없어 피크가 0dBFS를 그냥 뚫는다 (EP01 실측 **트루피크 +4.2dBFS**) | 게인은 캡컷이 아니라 **파일에 굽는다**. `audio_normalize.py` 가 전체 프로그램 기준 균일 게인 + 트루피크 리미터를 걸고, 캡컷 볼륨은 1.0 | **코드** |
| 3-7 | 장면마다 소리 크기가 들쭉날쭉 | 파일별로 정규화했다 | **전체를 이어붙여 한 번 재고 균일 게인**을 건다. 장면별 정규화는 조용해야 할 대목까지 끌어올려 강약을 뭉갠다 | **코드** |
| 3-8 | 정규화를 두 번 돌리니 값이 틀어짐 | 변환은 원본에서 하는데 게인은 현재 파일 기준으로 쟀다 | 게인은 **변환의 입력이 될 파일**(백업 원본)에서 잰다. 원본에서 다시 굽는 방식이라 몇 번 돌려도 세대열화가 없다 | **코드** |

## 4단계 — FLOW

| # | 증상 | 원인 | 규칙 | 어디에 |
|---|---|---|---|:--:|
| 4-1 | 9:16 이 자꾸 16:9 로 돌아감 | **Esc 가 비율 선택을 취소**한다 | 패널은 **입력창 클릭**으로 닫는다 | 지침 (04) |
| 4-2 | 엉뚱한 이미지가 프롬프트에 붙음 | 영상이 완성될 때마다 **이미지 필터가 풀려** 타일이 밀린다 | 그리드 금지. **`시작` → 에셋 검색** (`02a.장면구분.json` 의 `search`) | 지침 (04) |
| 4-3 | 크롬이 연속 다운로드를 차단 | 브라우저 보호 기능 | 사이트 권한에서 자동 다운로드 허용 | 지침 (04) |
| 4-4 | 다운로드한 컷이 24개였다 (cut5 누락) | 필름스트립을 눈으로 셌다 | **길이 지문 검사**로 대조한다 | **코드** |
| 4-5 | 같은 컷 영상이 두 개 | 이미지 두 장에서 각각 뽑았다 | 영상 전에 **이미지 25장을 먼저 확정** (`02c.이미지선택.md`) | 지침 (04) |
| 4-6 | 봇 제재 우려 | 큐에 몰아넣었다 | **1컷 완성 → 다운로드 → 다음 컷** | 지침 (04) |
| 4-7 | ★ 프롬프트를 넣었는데 "프롬프트를 입력해야 합니다" | JS `execCommand('insertText')` 는 DOM 에만 쓰고 React 상태에 반영이 안 된다 | **실제 키보드 타이핑만 통한다.** 1,500자도 한 번에 들어간다 | 수동 |
| 4-8 | 확장 연결이 끊겨 배치가 실패 | 일시적 | **끊긴 배치도 실제로는 실행돼 있을 수 있다.** 재전송 전에 그리드를 먼저 확인할 것 (EP01에서 중복 2건 발생) | 수동 |
| 4-9 | ★ 에셋 목록에서 원하는 컷을 못 찾음 | 제목이 자동 생성이라 비슷한 게 겹친다("Qin Shi Huang mausoleum" 이 신·구 둘 다) | 목록에서 **`이미지` 필터**를 켜면 영상이 안 섞여 순서가 고정된다. 그래도 안 되면 **검색창에 유물명 키워드**를 넣는다(제목만 매칭). 고르면 **미리보기로 반드시 눈 확인** | 수동 |
| 4-10 | ★ 영상 생성이 안전 필터에 막힘 | I2V 프롬프트에 `collapsed figures`, `dying torch` 처럼 죽음을 직접 묘사했다 | 같은 그림이라도 **문구를 정적으로** 바꾼다. "쓰러진 인물" → "방 안의 모든 것이 완전히 정지해 있다", "꺼져가는 횃불" → "마지막 연기 한 줄기가 피어오른다" | 지침 (04) |
| 4-11 | ★ 진행표는 25컷 완료인데 실제 교정 영상은 24개 | 신·구 영상이 한 그리드에 섞여 총개수만 보고 완료 처리했다 | 진행표를 믿지 말고 **교정 원본 이미지 ID 25개와 생성 영상 ID를 1:1 대조**한다. EP01은 이 검사로 컷18 누락을 찾아 재생성했다 | 코드/수동 |
| 4-12 | 자동 입력 후 전송 버튼이 비활성 | DOM에는 글자가 보여도 React 입력 상태는 비어 있었다 | Playwright는 `fill()` 대신 실제 키 이벤트가 발생하는 **`pressSequentially()`** 를 사용하고, 전송 전 `aria-disabled=false`를 확인한다 | 코드/수동 |
| 4-13 | 중국 유적인데 유럽인·서양 건축이 나온다 | 시대·지역 이름만 쓰고 인물·복식·건축·금지 문화권을 잠그지 않았다 | `visual_lock`의 문화권·인물·유적 지문·금지 문화권을 이미지에 먼저 고정하고 실패 이미지는 폐기한다 | 지침 (02·04) |
| 4-14 | 진시황릉이 일반 둥근 무덤처럼 나온다 | 특정 유적 대신 `ancient tomb` 같은 보편명사로 생성했다 | 실물·공식 도면·발굴 배치를 레퍼런스로 쓰고 대상 고유 지문 3개 이상이 보이지 않으면 I2V로 넘기지 않는다 | 지침 (02·04) |
| 4-15 | 영상에서 유물·건축 형태가 바뀐다 | T2V로 본편을 만들거나 시작 이미지가 잠기기 전에 영상화했다 | 최종 본편은 `I2V_LOCKED`만 사용하고 문명·유물·공간을 먼저 승인한 3D 디오라마 이미지에서 시작한다 | 지침/검증기 |
| 4-16 | 카메라와 3D 그래픽이 나레이션보다 먼저 답을 보여 준다 | 실제 TTS를 구절별 시간 비트로 나누지 않았다 | `tts_beats`에서 구절·카메라·행동·그래픽의 시작/끝을 잠근 뒤 프롬프트를 쓴다 | 지침/검증기 |
| 4-17 | 모든 장면에 빨간 화살표가 떠서 게임 UI처럼 보인다 | 장면의 질문·물성 대신 한 가지 효과를 복사했다 | 바람·물질·힘·치수·스캔·단면·위험마다 기능·재질·색을 다르게 고르고 월드 가림·시차를 검수한다 | 지침 (02V·04) |

## 5단계 — 캡컷

| # | 증상 | 원인 | 규칙 | 어디에 |
|---|---|---|---|:--:|
| 5-1 | ★ 드래프트가 안 열림 + 투탕카멘 표지가 뜸 | 템플릿 폴더를 **`copytree`** 해서 `draft.extra`·`key_value.json`·`Timelines/`·`subdraft/`·`crypto_key_store.dat` 같은 이전 상태가 딸려 왔다 | **폴더를 통째로 복사하지 않는다.** 새 폴더를 만들고 `draft_content.json` / `draft_meta_info.json` / `draft_cover.jpg` **3개만** 쓴다 | **코드** |
| 5-2 | 표지가 이전 프로젝트 것 | `draft_cover.jpg` 를 그대로 가져왔다 | **첫 클립에서 ffmpeg 로 새로 뽑는다** | **코드** |
| 5-3 | 소재가 깨져 보임 | `draft_materials` 등록부가 이전 파일을 가리켰다 | 등록부를 **실제 미디어로 재구성** + `draft_timeline_materials_size_` 재계산 | **코드** |
| 5-4 | 클립이 안 붙음 | 상대 경로를 썼다 | 모든 경로 **`.resolve()`** | **코드** |
| 5-5 | 그룹이 남아 오류 | 템플릿의 `group_container` 가 유지됐다 | **`group_container = None`** | **코드** |
| 5-6 | ★ "자막이 TTS 속도에 전혀 안 맞다" | 자막을 **글자수 비율로 배분**했다 | **ElevenLabs 강제 정렬**(`/v1/forced-alignment`)로 문자 단위 실측 시각을 받는다 → `자막_싱크.json` | **코드** |
| 5-7 | 자막이 "2천 / 년을", "갈아 / 넣었죠" 로 잘림 | 글자수만 보고 끊었다 | 끊으면 안 되는 덩어리를 **미리 붙인다** (숫자+단위, 본용언+보조용언). 어절 경계에서만 자른다 | **코드** |
| 5-8 | ★ 문장 끝에 「보죠.」 3자 자막이 0.26초 떴다 사라짐 | 앞에서부터 욕심껏 채우니(greedy) 나머지가 꼬리로 남았다 | 문장 전체를 보고 **줄 길이를 고르게** 나눈다(DP, 비용 = Σ(길이−목표)²). 마지막 줄도 똑같이 비용을 문다 | **코드** |
| 5-9 | 낡은 드래프트를 검수하고 통과했다고 착각 | `capcut_build` 는 `EP01_진시황릉_자동`, 검수기는 `EP01 진시황릉` 을 봤다 | **두 도구가 같은 이름 규칙을 쓴다.** 이름이 갈리면 지난 회차를 검수하게 된다 | **코드** |
| 5-10 | 눈으로 봐선 싱크가 맞는지 알 수 없다 | 검수 기준이 없었다 | **`verify_draft.py`** 가 41개 항목을 전수 검사한다. 내보내기 전에 반드시 돌린다 | **코드** |
| 5-11 | 캡컷 GUI 없이는 내보낼 수 없다 | 내보내기를 사람 손에 묶어 뒀다 | **`render_final.py`** 가 같은 소재로 직접 굽는다. 자막 크기는 투탕카멘 완성본에서 실측 캘리브레이션(캡컷 size 12 = 74px, 오차 1%) | **코드** |
| 5-11a | 내가 캡컷 화면을 못 본다 | 권한 시스템은 `CapCut` 을 버전 없는 런처 경로로 잡는데 실제 프로세스는 `Apps\9.2.8.3974\CapCut.exe` 에서 돈다. 경로가 안 맞으면 스크린샷에서 **마스킹**된다 | 재요청해도 같은 경로만 온다. **캡컷 조작에 기대지 말고 `render_final.py` 로 굽는다.** 캡컷은 사람이 눈으로 확인할 때만 쓴다 | 수동 |
| 5-12 | 폰트 파일이 있는데 파이썬이 못 연다 | 캡컷 캐시의 `KCC간판체.ttf` 가 디스크에 **NFD**로 저장돼 있어 리터럴(NFC)과 다르다. PIL·libass 는 비ASCII 경로 자체를 못 연다 | 폴더를 `iterdir()` 해서 실제 이름을 얻고, **ASCII 경로로 복사해** 쓴다 | **코드** |
| 5-13 | 영상은 예쁘지만 구조 원리가 안 보인다 | 장면 분석 없이 Flow 영상만 만들었다 | 생성 전에 `VEO_INTEGRATED_3D`의 시작·경유·도착·가림·시간 단계를 잠그고 영상 안에 설명모션을 통합한다 | 지침 (02V·04) |
| 5-14 | 모션 라벨이 자막을 가리거나 엉뚱한 부품을 가리킨다 | 좌표를 실제 프레임에서 검수하지 않았다 | 모션 대상 컷마다 중간 프레임을 먼저 검수하고, 최종 mp4에서도 같은 시각을 다시 추출해 2차 검수한다 | 지침 (05) |

## 6~7단계 — 메타 · 썸네일 · 업로드

| # | 증상 | 원인 | 규칙 | 어디에 |
|---|---|---|---|:--:|
| 6-1 | 썸네일 한글이 뭉개짐 | 이미지 모델에 글자를 시켰다 | **배경만 뽑고 글자는 `thumbnail_build.py` 가 얹는다** | **코드** |
| 6-2 | 제목이 인물 얼굴을 덮음 | 배경 상단이 안 비었다 | 배경 프롬프트의 `[구도]` — **상단 33%를 비운다** | 지침 (06) |
| 6-3 | 매번 GPT에 붙여넣음 | 자동화가 아니다 | 글꼴·색·크기는 `채널설정.json`. 명령 한 줄로 합성 | **코드** |
| 7-1 | 브라우저 제어가 너무 느림 | 업로드를 UI로 했다 | **YouTube Data API v3 `videos.insert`.** 업로드 1,600 쿼터 / 하루 10,000 | **코드** |
| 7-2 | 근거 없는 내용을 단정해서 서술 | 팩트체크 결과가 반영 안 됐다 | 설명란에 **재현·추정 고지**. 없으면 업로더가 `--run` 을 막는다 | **코드** |
| 7-3 | AI 고지 누락 | 잊었다 | 설명에 "AI" 가 없으면 업로더가 막는다 | **코드** |
| 7-4 | 합성 콘텐츠 고지 / 첫 댓글 고정 | **API 에 해당 필드가 없다** | 업로드 후 스튜디오에서 30초. 스크립트가 링크를 찍어 준다 | 수동 |
| 7-5 | ★ 엉뚱한 채널에 올라감 | 구글 계정이 여러 개라 동의 화면에서 다른 계정·브랜드채널을 골랐다 | `채널설정.json` 의 **`업로드.채널명`** 과 토큰의 채널명이 다르면 업로더가 **올리기 직전에 중단**한다 | **코드** |
| 7-6 | `앱이 차단됨` / `access_denied` | OAuth 동의 화면의 **테스트 사용자**에 계정을 안 넣었다 | 07-0 ③에서 업로드에 쓸 계정을 테스트 사용자로 추가 | 지침 (07) |
| 7-8 | ★ 동의는 됐는데 `localhost 연결 거부`(ERR_CONNECTION_REFUSED) | 구글이 인증 후 `http://localhost:<포트>` 로 돌려보내는데 **파이썬이 이미 죽어 있었다.** 포트는 실행마다 바뀌어서 **이전 실행의 URL 은 죽은 주소**다 | `--auth` 를 돌린 뒤 **그 창을 닫지 말고**, **이번 실행에서 나온 URL** 로 동의를 끝낸다. 도구가 이제 경고를 찍고 10분까지 기다린다 | **코드** |
| 7-9 | 인증 도중 400 Bad Request (`delegation?authuser=3`) | 브라우저에 계정이 여러 개 로그인돼 있어 흐름 중간에 계정을 바꾸니 인덱스가 어긋났다 | **시크릿 창**에 URL 을 붙여넣는다. 로그인된 계정이 없어 혼선 자체가 사라진다 | 지침 (07) |
| 7-7 | 일주일 뒤 토큰 만료 | 동의 화면이 **테스트 모드**면 리프레시 토큰이 7일마다 죽는다 | `--auth` 를 다시 돌리거나, 동의 화면을 프로덕션으로 게시 | 지침 (07) |

## 환경

| # | 증상 | 원인 | 규칙 | 어디에 |
|---|---|---|---|:--:|
| E-1 | `UnicodeEncodeError: cp949` | 한국어 윈도우 콘솔 기본 인코딩 | 모든 도구가 `_config` 를 import 하는 순간 **stdout 을 utf-8 로 재설정**한다 | **코드** |
| E-2 | 비밀 스캔이 통과했다고 잘못 보고 | `git grep -e PATTERN --cached` 가 인자 순서 때문에 에러났는데 결과를 안 봤다 | **`--cached` 를 패턴보다 앞에.** 종료코드를 확인하고 보고한다 | 수동 |
| E-4 | PowerShell 에서 `cd` 가 "경로 없음" | 폴더명의 **`[채널]` 을 와일드카드로 해석**한다. `[채널]` 은 '채' 또는 '널' 한 글자 매칭 패턴이 된다 | **`Set-Location -LiteralPath "..."`** 를 쓰거나, 스크립트를 절대 경로로 직접 실행한다 | 지침 (README) |
| E-3 | API 키가 커밋될 뻔 | 지침 파일에 키를 적었다 | 키는 **`.env` 에만**. `.env`·`client_secrets.json`·`token.json` 은 `.gitignore` | **코드** |

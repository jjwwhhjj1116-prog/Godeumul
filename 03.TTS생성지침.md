# 3단계 — TTS 생성 (ElevenLabs)

02단계에서 나온 시각화 대본의 `[한국어 나레이션]` 줄을 장면별 mp3로 만든다.
이 단계의 산출물인 **`durations.json`이 5단계 캡컷 편집의 입력**이므로, 여기서
길이가 확정되어야 뒤가 굴러간다.

---

## 3-1. 설정

값은 전부 `.env`에 있다. 코드나 문서에 키를 다시 적지 않는다.

| 항목 | 값 | 비고 |
|---|---|---|
| 보이스 | `wTGzPmtwk7nDNybbk0OL` — 오늘을 만든 이름들_역사해설_V3 | 이 채널 나레이션 |
| 모델 | `eleven_multilingual_v2` | |
| Speed | `1.05` | 쇼츠 이탈 방지용 약간 빠르게 |
| Stability | `0.4` | 낮게 잡아 톤에 생기를 준다 |
| Similarity | `0.85` | 보이스 고정 |
| Style | `0.50` | 해설 톤의 억양 |
| Speaker Boost | `Enabled` | |
| Language Override | **`ko` (Korean)** | ★ 켜야 한국어 발음이 안정된다 |
| Output | `MP3 44.1kHz 128kbps` | |

주인님이 확정한 설정값이다 (2026-08-23).

> **톤 기준** — 아나운서 낭독체가 아니라 *자료를 많이 본 사람이 옆에서 맛깔나게 설명해 주는* 톤.
> 대본은 `~죠 / ~거든요 / ~고요 / ~인 셈이죠 / ~라는 겁니다`와 담화 표지를
> 문맥에 맞게 섞어 리듬을 먼저 만든다. 세 어미를 기계적으로 순환하지 않는다.
> TTS는 그 말맛을 지우지 않되 과장된 연기나 광고식 고조를 덧붙이지 않는다.

### 구간별 속도 예외

**기본은 전 구간 `1.05`다.**

증거 상태가 바뀌거나 중앙 미스터리에 도달한다고 해서 자동으로 속도를 낮추지 않는다.
대본에 의도된 정적·회수 구간이 있고 실제 청취에서 필요할 때만 해당 장면을 `1.0`으로
낮춘다. 속도 변경은 장면 ID와 함께 기록하며, 해당 장면만 다시 생성한다.

※ 3분 상한이 빠듯한 회차에서는 속도를 올려 맞추지 말고 **대본을 줄인다.**

### TTS 생성 전 말맛·문맥 게이트

TTS 크레딧을 쓰기 전에 Humanizer KO의 문서→문단→문장 순서로 원문을 읽고,
우리 채널의 다섯 검문을 모두 수행한다.

1. 문단마다 역할과 새로 주는 정보를 적고 전체 정보 흐름을 검사한다.
2. 모든 인접 문단·문장의 인과·대조·전환 관계가 앞 내용과 맞는지 검사한다.
3. `그런데·하지만·그래서·그러니까` 같은 접속어를 지운 문장과 비교해 실제 기능을 확인한다.
4. 이미 설명한 정보와 사실상 같은 문장을 찾아 삭제·통합하거나 의도적 반복으로 승인한다.
5. 지적을 모두 해결한 현재 대본의 SHA-256으로 `01.문맥검수.json`을 잠근다.

```bash
python tools/script_tone_check.py 산출물/EP01_진시황릉/01.대본.txt
python tools/script_tone_check.py 산출물/EP01_진시황릉/02.시각화.txt
```
```bash
python tools/script_context_gate.py 산출물/EP01_진시황릉/01.대본.txt
```

`~습니다/~입니다`형이 세 문장 연속이거나 대화형 종결이 지나치게 적으면 생성하지 않는다.
`tts_generate.py`는 문맥 잠금이 없거나 대본 SHA, 다섯 PASS, 접속어 검수, 중복 검수가
하나라도 어긋나면 드라이런과 실제 생성 모두 중단한다. 대본을 한 글자라도 고쳤으면
기존 잠금을 재사용하지 말고 다시 읽고 잠근다.

---

## 3-1b. 발화 속도 (실측)

| 측정 | 값 | 조건 |
|---|---|---|
| 투탕카멘 편 (캡컷 자막) | 8.07 자/초 | 장면 간 간격 포함 |
| **EP01 진시황릉 (순수 TTS)** | **9.35 자/초** | 간격 없음, speed 1.05 |
| **기획용 보수값** | **8.0 자/초** | 편집 후 간격을 감안한 값 |

순수 TTS는 9.35자/초로 나오고, 캡컷에서 장면 사이 간격이 붙으면 8자/초 언저리가
된다. 기획할 때는 **8.0**을 쓴다.

대본 길이로 영상 길이를 미리 가늠할 때 이 값을 쓴다.

```
예상 초 = 대본 글자수(공백 포함) ÷ 8.0
```

| 대본 글자수 | 예상 길이 |
|---:|---|
| 1,000 | 2:05 |
| **1,240** | **2:35** (EP01 실측 2:11) |
| 1,400 | 2:55 |
| 1,440 | 3:00 ← 상한 |

★ **1,400자를 넘기지 마라.** 3분 상한에 걸린다.

> 이전 지침에 적혀 있던 5.8자/초는 추정치였고 실제보다 느렸다.
> 그 값으로 계산하면 멀쩡한 대본을 불필요하게 잘라내게 된다.

---

## 3-2. 실행

### 원문 자막과 TTS 발음문 분리

대본·자막에는 정상 맞춤법 원문을 보존한다. ElevenLabs API에는
`tts_pronunciation.json`을 적용한 **TTS 전용 치환문**만 보낸다.

```text
대본·자막 원문: 방아쇠를 조사했습니다.
ElevenLabs 입력: 방아쐬를 조사했습니다.
```

- `01.대본.txt`, `02.시각화.txt`의 나레이션 원문을 발음 표기로 고치지 않는다.
- `durations.json`에는 `text` 원문, `tts_text` 치환문, `pronunciation_changes` 이력을
  함께 저장한다.
- 숫자는 TTS 입력에서 한글 수사로 풀지만 자막은 기존 숫자 표시 규칙을 유지한다.
- `AI`, `PDF`, `SNS` 등 등록된 약어만 발음사전으로 푼다.
- 동음이의어나 문맥에 따라 발음이 달라지는 말은 자동 치환하지 않는다.
- 새 오발음은 실제 청취 후 사전에 추가한다. 추측으로 대량 등록하지 않는다.
- 발음사전이 바뀌면 TTS 캐시 서명도 바뀌어 해당 장면을 다시 생성한다.

현재 실측 고정 항목: `방아쇠 → 방아쐬`. ElevenLabs가 원문을 `바라세`로 읽은 사고를
막기 위한 항목이다.

```bash
python tools/tts_generate.py "산출물/EP01_진시황릉/02.시각화.txt"
```

**기본은 드라이런이다.** 장면 수·글자 수·예상 과금만 찍고 크레딧을 쓰지 않는다.
출력이 맞는지 확인한 뒤에 실제로 만든다.

```bash
python tools/tts_generate.py "산출물/EP01_진시황릉/02.시각화.txt" --run
```

| 옵션 | 용도 |
|---|---|
| `--run` | 실제 생성 (크레딧 소모) |
| `--only 3,7,12` | 특정 장면만 재생성 |
| `--force` | 캐시 무시하고 전부 다시 |
| `--outdir <경로>` | 출력 폴더 지정 (기본: 대본 옆 `audio/`) |
| `--pronunciation-dictionary <파일>` | 기본 `tts_pronunciation.json` 대신 회차별 사전 사용 |
| `--context-script <파일>` | 문맥 검수를 받은 원문 대본 경로 |
| `--context-review <파일>` | `01.문맥검수.json` 경로 |

### 크레딧 안전장치

- 텍스트가 안 바뀐 장면은 **자동으로 건너뛴다**. 재실행해도 크레딧 0.
- 보이스 파라미터를 바꾸면 서명이 달라져 자동 재생성된다.
- 응답이 1KB 미만이면 실패로 처리한다.
  (무협 파이프라인에서 0바이트 mp3가 만들어져 ffmpeg이 죽고 전체가 멈춘 적이
  있다. 그 사고를 여기서 차단한다.)

---

## 3-3. 산출물

```
산출물/EP01_진시황릉/audio/
├─ 001.mp3
├─ 002.mp3
├─ ...
└─ durations.json      ← ★ 5단계의 입력
```

`durations.json` 구조:

```json
{
  "voice_id": "wTGzPmtwk7nDNybbk0OL",
  "scene_count": 28,
  "total_duration": 154.2,
  "scenes": {
    "1": {
      "file": "001.mp3",
      "duration": 5.24,
      "chars": 62,
      "text": "방아쇠를…",
      "tts_text": "방아쐬를…",
      "pronunciation_changes": [
        {"kind": "dictionary", "source": "방아쇠", "target": "방아쐬"}
      ]
    }
  }
}
```

---

## 3-4. 확인할 것

- [ ] 장면 수가 02단계 컷 시트와 일치하는가
- [ ] `script_context_gate.py`가 현재 대본 SHA로 통과했는가
- [ ] 모든 접속어를 삭제한 버전과 비교했고 앞 문장과 실제 관계가 맞는가
- [ ] 중복 후보와 미해결 문맥 지적이 0개인가
- [ ] `total_duration`이 **180초 미만**인가 (쇼츠 상한)
- [ ] 넘치면 대본으로 돌아가 문장을 줄인다. TTS 속도로 때우지 않는다
- [ ] 숫자·단위가 제대로 읽히는가 (`36km` → "삼십육 킬로미터")
      → 안 읽히면 대본에 한글로 풀어 쓴다
- [ ] 한자·괄호·특수문자가 대본에 남아 있지 않은가
      (대본 지침 §2 "텍스트 순수성" 위반이면 TTS가 이상하게 읽는다)
- [ ] 드라이런의 `TTS 치환` 줄에서 원문과 치환문이 의도대로 분리됐는가
- [ ] 실제 생성 뒤 장면별 미리듣기로 고유명사·받침·숫자·약어를 확인했는가
- [ ] 자막에는 발음 표기가 아니라 정상 맞춤법 원문이 표시되는가
- [ ] 오발음 장면은 승인하지 않고 치환 수정 후 `--only`로 다시 생성했는가

---

## 3-5. 주의

- 이 채널은 **나레이션 1인 단일 화자**다. 무협 파이프라인의 `[화자|감정]`
  태그 구조를 쓰지 않는다.
- 무협 파이프라인(`TTS+영상 변환기/`)과 **같은 ElevenLabs 계정**을 쓴다.
  월 한도(creator 약 197k자)를 공유하므로, 대량 생성 전에 잔여량을 확인한다.
- 이 채널 작업이 무협 파이프라인 코드를 건드리지 않는다. 완전히 별도 폴더·
  별도 스크립트다.

# 7단계 — 업로드

**브라우저를 제어하지 않는다.** YouTube Data API v3 로 파일을 그대로 쏜다.
브라우저 자동화는 화면이 바뀌면 깨지고 느리다. API 는 안 깨진다.

```bash
python tools/youtube_upload.py 산출물/EP01_진시황릉                    # 점검만
python tools/youtube_upload.py 산출물/EP01_진시황릉 --run              # 비공개로 올림
python tools/youtube_upload.py 산출물/EP01_진시황릉 --run --공개 예약 --시각 "2026-08-25 19:00"
```

---

## 7-0. 최초 1회 설정 — API 발급

**계정이 여러 개면 여기서 대부분 틀어진다.** 어느 계정으로 로그인하느냐가
"어느 채널에 올라가느냐"를 결정한다. 순서대로 따라간다.

### 준비 — 먼저 정할 것

| 질문 | 답 | 왜 중요한가 |
|---|---|---|
| 이 채널을 소유한 **구글 계정**이 뭔가 | (예: `xxx@gmail.com`) | 4단계 동의를 **이 계정으로** 해야 한다 |
| 그 계정에 채널이 여러 개인가 | 브랜드 계정 여부 | 여러 개면 6단계에서 반드시 확인 |

> 확인법: https://www.youtube.com/account → 우상단 프로필 → 계정 전환 목록.
> **[고대유물의 비밀] 이 보이는 계정**이 정답이다.

브라우저에 여러 구글 계정이 로그인돼 있으면 동의 화면에서 엉뚱한 계정이 잡힌다.
**시크릿 창을 쓰거나, 다른 계정을 전부 로그아웃한 뒤** 진행하는 게 가장 확실하다.

---

### ① 프로젝트 만들기

https://console.cloud.google.com 접속. **우상단 계정이 위에서 정한 그 계정인지
먼저 확인한다.**

상단 프로젝트 선택기 → `새 프로젝트` → 이름 `godeumul-upload` → 만들기.
만든 뒤 **그 프로젝트가 선택돼 있는지** 다시 본다. 다른 프로젝트에 API를 켜는 게
흔한 실수다.

### ② YouTube Data API v3 켜기

`API 및 서비스` → `라이브러리` → "YouTube Data API v3" 검색 → **사용** 클릭.

> `YouTube Analytics API` 나 `YouTube Reporting API` 가 아니다. **Data API v3** 다.

### ③ OAuth 동의 화면

`API 및 서비스` → `OAuth 동의 화면`

| 항목 | 값 |
|---|---|
| User Type | **외부** |
| 앱 이름 | 아무거나 (예: 고대유물 업로더) |
| 사용자 지원 이메일 | 본인 계정 |
| 개발자 연락처 | 본인 계정 |
| 범위(Scope) | **추가하지 않는다.** 코드가 요청한다 |

**★ 테스트 사용자** 단계에서 `+ ADD USERS` → **위에서 정한 구글 계정 주소를 추가**한다.
이걸 빼면 4단계에서 `앱이 차단됨` 이 뜬다. 계정이 여러 개면 **업로드에 쓸 계정만**
넣으면 된다.

> 게시 상태는 **테스트**로 두면 된다. 본인만 쓰므로 구글 심사가 필요 없다.
> 테스트 모드의 리프레시 토큰은 **7일마다 만료**된다 — 만료되면
> `python tools/youtube_upload.py --auth` 를 다시 한 번 돌리면 된다.
> 매주 하기 싫으면 동의 화면을 `프로덕션`으로 게시한다(민감하지 않은 범위라
> 대개 심사 없이 바로 된다).

### ④ 사용자 인증 정보 (client_secrets.json)

`API 및 서비스` → `사용자 인증 정보` → `+ 사용자 인증 정보 만들기`
→ **OAuth 클라이언트 ID** → 애플리케이션 유형 **데스크톱 앱** → 만들기
→ **JSON 다운로드**

받은 파일을 저장소 루트에 **`client_secrets.json`** 이라는 이름으로 둔다.

```
[채널]고대유물의 비밀/
├─ client_secrets.json     ← 여기
├─ 채널설정.json
└─ tools/
```

> 이건 **API 키가 아니라 OAuth 클라이언트**다. 유튜브 업로드는 "누구의 채널에
> 올릴지"를 알아야 해서 단순 API 키로는 안 되고 계정 동의가 필요하다.
> (API 키는 공개 데이터 조회용이다.)

### ⑤ 라이브러리 설치

```bash
pip install google-api-python-client google-auth-oauthlib google-auth-httplib2
```

### ⑥ 로그인 — ★ 계정을 고르는 순간

```bash
python tools/youtube_upload.py --auth
```

브라우저가 열린다. 여기서 **반드시 확인할 것:**

1. **계정 선택 화면** — 위에서 정한 그 계정을 고른다. 목록에 없으면
   `다른 계정 사용` 으로 로그인한다
2. `Google에서 확인하지 않은 앱` 경고 → `고급` → `(앱 이름)(으)로 이동`
   (내가 만든 앱이라 정상이다)
3. **채널 선택 화면이 뜨면** — 브랜드 계정이 여러 개일 때 나온다.
   **[고대유물의 비밀] 을 고른다.** 여기서 잘못 고르면 다른 채널에 올라간다
4. 권한 요청 → 허용

성공하면 이렇게 찍힌다:

```
  토큰 저장 -> ...	oken.json  (이제 브라우저는 다시 안 열립니다)

  채널   : 고대유물의 비밀      ← ★ 이 줄이 맞는지 반드시 확인
  구독자 : 0
  영상   : 0편
```

**채널명이 다르면 잘못 인증한 것이다.** `token.json` 을 지우고 ⑥을 다시 한다.

```bash
rm token.json
```

### 채널을 바꾸고 싶을 때

`token.json` 만 지우고 `--auth` 를 다시 돌리면 계정을 다시 고를 수 있다.
프로젝트나 `client_secrets.json` 은 그대로 써도 된다 — **하나의 프로젝트로
여러 채널에 올릴 수 있다.** 다만 쿼터 10,000/일은 프로젝트 단위로 공유된다.

채널마다 토큰을 따로 두고 싶으면 파일명을 바꿔 보관한다:

```bash
mv token.json token.고대유물.json
```

> `client_secrets.json` 과 `token.json` 은 **절대 커밋하지 않는다.** 계정 접근
> 권한 그 자체다. `.gitignore` 에 이미 들어 있다.

---

### 쿼터

| 작업 | 쿼터 |
|---|---|
| 영상 업로드 (`videos.insert`) | **1,600** |
| 썸네일 설정 (`thumbnails.set`) | 50 |
| 하루 한도 (기본) | 10,000 |

**하루 6편**까지 올라간다. 이 채널은 하루 1편이므로 남는다.
초과하면 다음 날 **태평양시 자정**(한국 시간 오후 4~5시)에 초기화된다.

### API 로 되는 것 / 안 되는 것

| 항목 | API | 비고 |
|---|:--:|---|
| 영상 업로드 | O | 이어올리기(resumable) 지원 |
| 제목·설명·태그·카테고리·언어 | O | |
| 공개 / 비공개 / 일부공개 | O | |
| 예약 발행 | O | `publishAt`, 반드시 비공개로 올린 뒤 예약 |
| 아동용 여부 | O | `selfDeclaredMadeForKids` |
| 썸네일 | O | **전화번호 인증된 계정만.** 2MB 이하 |
| **변형·합성 콘텐츠 고지** | **X** | ★ 필드가 없다. 업로드 후 스튜디오에서 켠다 |
| 재생목록 추가 | O | `playlistItems.insert` (50) |
| 고정 댓글 | 반쪽 | 댓글 작성은 되지만 **고정은 API 로 안 된다** |

→ 자동화되지 않는 건 **① 합성 콘텐츠 고지 ② 첫 댓글 고정** 둘뿐이다.
   업로드가 끝나면 스크립트가 스튜디오 링크를 찍어 준다. 30초면 끝난다.

### 잘 걸리는 오류

| 메시지 | 원인 | 대응 |
|---|---|---|
| `앱이 차단됨` / `access_denied` | 테스트 사용자에 계정을 안 넣었다 | ③의 테스트 사용자에 추가 |
| `채널: (다른 이름)` | 동의할 때 계정·채널을 잘못 골랐다 | `token.json` 지우고 `--auth` 다시 |
| `quotaExceeded` | 하루 10,000 초과 | 태평양시 자정까지 대기 |
| `youtubeSignupRequired` | 그 계정에 유튜브 채널이 없다 | 유튜브에서 채널을 먼저 만든다 |
| `Token has been expired or revoked` | 테스트 모드 7일 만료 | `--auth` 다시. 잦으면 프로덕션 게시 |
| `forbidden` (썸네일만 실패) | 계정 전화번호 미인증 | 유튜브 설정에서 인증 |

---

## 7-1. 메타데이터 파일

에피소드 폴더에 **`06.메타.json`** 을 둔다. 업로더가 이걸 읽는다.

```json
{
 "제목": "40자 이내. 유물명 포함. 정답 금지",
 "설명": "타임라인 + 참고 출처 + 고지 + 댓글 유도 질문",
 "태그": ["최대 5개"],
 "고정댓글": "업로드 후 손으로 붙여넣을 문구",
 "썸네일문구": "유물명"
}
```

업로더가 **자동으로 막는 것** (하나라도 걸리면 `--run` 이 거부된다):

- 제목 40자 초과 / 100자 초과 / `< >` 포함
- 설명 5,000자 초과
- 해시태그·태그가 `채널설정.json` 의 `업로드.해시태그최대` 초과
- 설명에 **AI 재현물 고지가 없음**
- `00.팩트체크.md` 가 있는데 설명에 **재현·추정 고지가 없음**
- 완성본 mp4 를 못 찾음

---

## 7-1. 업로드 전 최종 게이트

하나라도 걸리면 올리지 않는다.

### 영상
- [ ] 1080×1920 · 30fps · 3분 미만
- [ ] 첫 3초에 후킹이 완결
- [ ] 하단 18%에 자막·정보 없음
- [ ] 마지막 프레임이 검게 끝나지 않음
- [ ] 오디오 끊김·겹침 없음

### 내용
- [ ] 대본 5챕터 필수 문장이 전부 들어갔는가
  - Ch1 `"여기 [대상]이 있습니다."`
  - Ch3 `"여러분, 이거 정말 미치고 팔짝 뛸 노릇 아니겠습니까?"`
  - Ch4 `"이때 비상한 ~이 등장합니다."` 또는 `"그래서 발상을 뒤집습니다."`
  - Ch5 `"시간 속에 잠든 세기의 유물, 땅속에 묻혀 있던 위대한 유산, 바로 [대상]의 비밀이었습니다."`
- [ ] **출처를 댈 수 있는 내용만 있는가** (외계인·초고대문명 가설 0건)
- [ ] 챕터 4에 단면·분해·원리 컷이 전부 있는가

### 메타
- [ ] 제목에 정답이 안 들어갔는가
- [ ] 제목 40자 이내 + 유물명 포함
- [ ] 설명란에 타임라인 + 참고 출처
- [ ] 해시태그 5개 이하
- [ ] 썸네일에 유물 이름만

---


---

## 7-3. 업로드 설정

| 항목 | 값 | 어디서 |
|---|---|---|
| 형식 | Shorts (9:16, 3분 미만 자동 인식) | — |
| 공개 | 예약 업로드 | `--공개 예약 --시각` |
| 카테고리 | 교육 (27) | `채널설정.json` |
| 언어 | 한국어 | `채널설정.json` |
| 아동용 | **아니오** | `채널설정.json` |
| 자막 | 영상에 구워져 있으므로 별도 업로드 안 함 | — |
| 댓글 | 허용 (댓글이 이 채널의 지표다) | 기본값 |

### 업로드 직후 손으로 할 일 (딱 2개)

1. **합성 콘텐츠 고지** — 스튜디오 → 해당 영상 → `변형된 콘텐츠 또는 합성 콘텐츠` → 예
2. **첫 댓글 고정** — `06.메타.json` 의 `고정댓글` 을 붙여넣고 고정

스크립트가 끝나면서 스튜디오 링크를 찍어 준다.

---

## 7-4. AI 콘텐츠 고지

이 채널은 3D 생성 영상을 쓴다. 설명란 마지막에 반드시 한 줄:

```
※ 본 영상의 시각 자료는 AI로 제작된 3D 재현물입니다.
```

역사적 사실은 실제 자료에 기반하되, **화면은 재현물**이라는 점을 명확히 한다.
고증 채널에서 이걸 빼면 신뢰가 무너진다. 업로더가 이 문구의 유무를 검사한다.

---

## 7-5. 업로드 후

| 시점 | 할 일 |
|---|---|
| 즉시 | 합성 콘텐츠 고지, 첫 댓글 고정, 모바일에서 재생 확인 |
| 1시간 | 노출 클릭률(CTR) 확인 — 4% 미만이면 썸네일 교체 |
| 24시간 | 평균 시청 지속률 확인 — **어디서 떨어지는지**가 핵심 |
| 7일 | 아래 기록표에 남긴다 |

### 회차 기록표 (`산출물/기록.md` 에 누적)

```
| 회차 | 유물 | 길이 | 조회 | CTR | 평균시청률 | 이탈 지점 | 메모 |
|---|---|---|---|---|---|---|---|
| EP01 | 진시황릉 | 2:34 | | | | | |
```

**이탈 지점이 이 채널의 유일한 개선 신호다.**

| 이탈 위치 | 원인 | 다음 회차 수정 |
|---|---|---|
| 0~3초 | 후킹 실패 | Ch1 첫 문장을 더 짧게. 유물을 즉시 보여줄 것 |
| Ch2 중반 | 건설 서사가 지루함 | Ch2를 줄이고 Ch3을 앞당길 것 |
| Ch3 직전 | 난관이 남 일 같음 | 딜레마를 더 구체적인 물리 문제로 |
| **Ch4 중반** | ★ 메커니즘이 이해 안 됨 | 단면·분해 컷을 더 쪼갤 것. 가장 심각한 신호 |
| Ch5 | 정상 | 무시 |

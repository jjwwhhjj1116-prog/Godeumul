# 4단계 — FLOW 이미지 생성 + 옴니 영상 생성

02단계에서 나온 프롬프트를 실제 이미지·영상으로 뽑는다.
무협 파이프라인의 Flow 핸드오프 방식(파일로 주고받기)을 그대로 쓴다.

---

## 4-1. 내보내기 파일 2종

02단계 대본에서 프롬프트만 뽑아 **빈 줄 하나로 장면을 구분한** txt 두 개를 만든다.
VEO Automation 확장이 이 형식을 그대로 먹는다.

```
산출물/EP01_진시황릉/
├─ flow_images.txt     ← [영어 이미지 프롬프트]만, 빈 줄 구분
└─ flow_videos.txt     ← [영상 프롬프트]만, 빈 줄 구분
```

**절대 규칙**
- `Scene 1` / `[장면 1]` 같은 라벨을 넣지 않는다. 확장이 프롬프트로 오인식한다.
- 장면 사이는 **빈 줄 정확히 1개**. 2개 이상이면 장면이 어긋난다.
- 순서가 곧 장면 번호다. 001, 002, 003… 순으로 다운로드된다.

---

## 4-2. 이미지 생성 (FLOW)

이미지는 단순 참고 그림이 아니라 I2V가 끝까지 보존할 **고증 잠금 프리미엄 축소모형
3D 디오라마 첫 프레임**이다. 영상보다 먼저 이미지에서 문명·인물·유물·유적·공간을
확정하고, 매크로 렌즈의 선택적 심도와 정교한 모형 가장자리로 실사가 아닌 디오라마임을
읽히게 한다. 플라스틱 장난감·로우폴리·테이블탑 게임 모형은 금지한다.

| 항목 | 값 |
|---|---|
| 비율 | **9:16 세로** — 툴 설정에서 직접 지정한다. 프롬프트 문구만으로는 안 걸린다 |
| 해상도 | 1080×1920 이상 |
| 장당 | 후보는 여러 개 생성할 수 있으나 장면표에는 **최종 시작 이미지 1개만 잠근다** |

### 본편 공통 시각 잠금

모든 본편 이미지 프롬프트는 아래를 포함한다.

- `premium museum-scale crafted archaeological 3D diorama, not live-action`
- 시대·왕조·지역·인물 외형·복식·도구의 문화권 잠금
- 실물 사진·공식 도면·발굴 자료에서 뽑은 대상 고유 지문 3개 이상
- 지형·봉토·부속 갱·발굴 구역 등 확인된 공간 관계
- 다른 인종·문화권·건축·무덤 형식·시대착오 요소의 명시적 배제
- 카메라가 움직일 전경·중경·후경과 3D 그래픽의 시작·경유·도착 표면
- `camera_path`의 진입점·이동 통로·도착점, 문 개방이면 경첩·문턱·뒤쪽 빈 공간,
  단면 진입이면 절단 이음선·지층 깊이·카메라가 통과할 빈 경로

큐브·절단 블록은 지층과 공간 관계를 설명할 때만 선택한다. 일반 장면에는 큐브 모양을
강제하지 않는다. 모든 장면의 공통 기준은 `PBR_MICROTEXTURE_HIGH_FIDELITY`다. 실제
재료, 미세 균열·알갱이·부식·식생, 거칠기·습윤 반사, 불규칙한 밀도 변화를 잠근다.

레퍼런스 입력은 가능하면 다음 세 역할로 분리한다.

1. 실제 유적·유물 사진 또는 공식 도면 — **형태·배치 소유자**
2. 시대·인물·복식 레퍼런스 — **문화권 소유자**
3. 승인된 3D 디오라마 이미지 — **재질 정밀도·조명 품질 소유자**

스타일 레퍼런스의 산·폭포·건물을 대상 유적에 복사하면 실패다. 첨부한 이끼 낀 산
레퍼런스는 미세 질감과 렌더 품질만 참조하며 진시황릉의 실제 황토·잔디·관목·다진 흙은
각자의 재질로 재현한다.

Nano Banana 2 설정에서 해상도를 선택할 수 있으면 승인본은 사용 가능한 최고 해상도,
권장 4K로 생성한다. 프롬프트 문자열의 `4K`만 믿지 않고 실제 결과 파일 크기를 확인한다.

`an ancient tomb`, `mysterious mound`, `old ruins`처럼 보편명사만으로는 생성하지 않는다.
진시황릉을 설명하면서 임의의 둥근 봉분이나 서양식 무덤이 나오면 후보로 보관하지도 말고
폐기한다. 중국 진대 인물이 유럽인 얼굴·서양 복식으로 나오면 영상 단계로 넘기지 않는다.

### 장면 유형별 재생성 기준

가장 자주 실패하는 순서대로다. 아래 증상이면 **고치지 말고 다시 뽑는다.**

| 장면 유형 | 실패 증상 | 대응 |
|---|---|---|
| **CUTAWAY** | 절단면이 닫혀 벽처럼 나옴 / 지층이 안 보임 / 내부가 어두움 | `the ground sliced open along a clean flat cut face` 를 문장 앞쪽으로 옮기고 재생성 |
| **EXPLODED** | 부품이 붙어 있음 / 배경이 생김 | `with visible gaps between them` + `plain neutral studio backdrop` 강조 |
| **DISCOVERY_ACTION** | 손·도구의 접촉점이 불명확 / 동작이 이미 끝나 있음 | 첫 프레임은 동작 직전으로 되돌리고 손·도구·접촉점을 중앙 안전 영역에 둔다 |
| **ARTIFACT_MACRO** | 표면이 플라스틱처럼 매끈함 / 형태가 실물과 다름 | 공식 실물 자료의 재료·손상·형태 앵커를 앞쪽으로 옮긴다 |
| **HISTORICAL_RECONSTRUCTION** | 시대·인물·복식이 무국적 | 고증 카드의 문명·인물·복식 앵커를 글자 그대로 넣는다 |
| **SITE_ESTABLISH / SPATIAL_MAP** | 특정 유적이 아닌 일반 무덤·피라미드처럼 보임 | 실물·도면 레퍼런스와 유적 고유 지문 3개를 앞쪽에 넣고 잘못된 무덤 형식을 네거티브로 잠근다 |
| **SEALED_UNKNOWN** | 미확인 내부에 보물·함정이 사실처럼 생김 | 내부를 불투명한 경계나 추상 스캔으로 되돌리고 구체 물체를 제거한다 |
| **문자·라벨** | 글자가 뭉개짐 | 이미지·Veo에서는 제거하고 정확한 문자만 자막 렌더에서 넣는다 |

> **이미지 모델에 라벨을 시키지 않는다.** 모든 이미지 프롬프트에
> `no text, no labels, no letters`를 넣는다. 3D 선·화살표는 Veo 영상에서, 정확한
> 문자·숫자는 자막 렌더에서 처리한다.

### 일관성 유지

같은 유물이 여러 장면에 나오면 **첫 장면에서 확정된 이미지를 레퍼런스로 물려** 다음
장면을 만든다. 형태 문장은 고증 카드에서 확정해 매 관련 장면에 반복한다.

### 이미지 승인 게이트

다음을 모두 눈으로 확인한 뒤에만 I2V로 보낸다.

- 대상이 실물·도면 레퍼런스와 같은 유적·유물로 식별된다.
- 시대·지역·인물 외형·복식·건축·재료가 고증 카드와 같다.
- 첫 동작의 손·도구·유물 접점이 실행 가능한 위치다.
- 줌인·줌아웃·돌리·오비트에 필요한 실제 깊이층과 가림 물체가 있다.
- 단면 공개 장면은 절단 평면과 확인된 지층·구조가 시작 이미지에 준비돼 있다.
- 문 개방 장면은 문·경첩·문턱·진입 공간이 실제 자료와 시작 이미지에 모두 존재한다.
- 디오라마가 실사처럼 보이지 않고 축소모형의 렌즈·심도·제작 가장자리가 읽힌다.
- 3D 그래픽이 붙을 시작·경유·도착 앵커가 한 카메라 축에서 읽힌다.

---

## 4-3. 영상 생성 (옴니)

승인된 3D 디오라마 이미지를 시작 프레임으로 삼는 **`I2V_LOCKED`**다. 텍스트만으로
같은 역사 장면을 새로 추측하게 만들지 않는다.

| 방식 | 본편 사용 |
|---|---|
| I2V | **필수** — 특정 인물·유물·유적·건축·단면·공간 관계를 전달하는 모든 장면 |
| T2V | 금지 — 아이디어 탐색용 프리비즈에만 사용, 최종 클립으로 납품하지 않음 |

도구에서 레퍼런스 재료나 시작·끝 프레임을 지원하면, 특정 유물·인물의 형태는 레퍼런스로
고정하고 큰 단면 전환은 승인된 끝 프레임까지 고정한다. 기능이 없는 모델에서는 한 장면을
둘로 나눠 각각 시작 이미지를 만든다.

장면표의 `모션 소유권`을 먼저 확인한다.

- `GENERATED_PHYSICS`: 흙·바람·물·연기·빛·도구 충격을 I2V 프롬프트에 포함
- `VEO_INTEGRATED_3D`: 장면에 맞춘 경로·바람·물질·힘·치수·스캔·단면·분해 그래픽을 월드 공간에 직접 생성
- `INFO_OVERLAY`: 이미지와 영상에서 문자·치수·라벨을 만들지 않음

장면표의 `모션 공간`도 함께 확인한다.

- `WORLD_3D`: 카메라 시차·구조물 가림·깊이 방향의 흐름을 Veo 장면 안에 생성한다.
- `SURFACE_2_5D`: 지도·벽면·평평한 표면에 붙는 선을 Veo가 표면 원근에 맞춰 생성한다.
- `SCREEN_INFO`: 숫자와 근거 배지만 생성 영상에서 제외하고 편집 단계에 맡긴다.

| 항목 | 값 |
|---|---|
| 방식 | Image-to-Video (시작 이미지 필수) |
| 길이 | 실제 TTS를 기준으로 **4·6·8·10초** 중 가장 짧은 충분한 값 |
| 비율 | 9:16 |
| 오디오 | **제거** — 3단계 TTS를 쓴다 |

### TTS-카메라 비트 잠금

생성 전에 실제 TTS를 구절별 초 단위로 나누고 `나레이션 / 카메라 / 주 행동 / 3D
그래픽`을 한 줄씩 대응시킨다. 질문에서 대상에 물리적으로 접근하고, 규모를 말할 때
돌리백·크레인으로 빠지며, 내부 관계를 말할 때 문턱·통로·절단면을 따라 이동한다.
공간 진입을 렌즈 `zoom`으로 대신하지 않는다. 3D 그래픽은 해당 설명 단어보다 먼저
답을 보여주지 않는다.

```text
0.0-0.3s narration A → immediate snap dolly toward the entry anchor
0.3-2.2s narration B → controlled handheld follow and rack focus at contact point
2.2-5.8s narration C → tracked push through the documented doorway or section seam
5.8-8.0s narration D → direction change, arrive at destination, 0.5s settle
```

각 신규 장면은 `camera_path`의 `entry_anchor / route / destination / speed_profile /
operator_style / depth_transition / pattern_interrupts / settle_point`를 먼저 잠근다.
발견·문 개방·단면 진입 장면은 첫 0.35초 안에 이동하고 2~4초 안에 속도·방향·크기
변화가 한 번은 있어야 한다. `vlog camera` 대신 `controlled documentary handheld
gimbal` 또는 `immersive POV dolly`를 쓴다.

### 물리·통합 3D 모션 프롬프트

`move dynamically`, `animated arrows`처럼 추상적으로 쓰지 않는다. 물리 모션은 아래
다섯 정보를, 설명 그래픽은 `02V.VEO_통합3D_설명모션.md`의 12개 분석 항목을 먼저 잠근다.

1. 무엇이 움직이는가 — 먼지, 바람결, 연기, 물, 모래, 도구
2. 어디서 시작하는가
3. 어떤 표면·통로를 따라가는가
4. 무엇과 만나 어떤 반응을 만드는가
5. 어디에서 끝나며 다음 장면에 무엇을 넘기는가

```text
The camera glides forward at walking speed through the narrow stone corridor.
Thin pale-ochre airflow streamlines enter from the lower shaft in staggered pulses,
curve tightly along the wall, pass behind the nearest pillar, and lift fine dust
before rising into the upper vent. Keep the flow anchored to the architecture.
Single continuous shot, no floating HUD, no text, no hard cut, no new objects.
```

그래픽의 형태·재질·색은 장면 기능에 맞춘다. 빨강을 공통색으로 쓰지 않는다.

- 바람: 옅은 온백색·황토 반투명 유선과 먼지·천의 반응
- 수은·물·모래: 실제 물질의 색·점도·반사·중력
- 힘: 청동·구리·호박색 접촉 펄스
- 치수: 배경 대비 중성색의 얇은 3D 틱과 선
- 스캔: 절제된 청록·청백색 측정파
- 위험·침입: 필요한 경우에만 제한된 진사색·주홍색

위험·침입 경로에서만 다음과 같은 문장을 쓸 수 있다.

```text
one restrained cinnabar-red semi-transparent 3D danger route ribbon with small
repeated directional arrowheads, anchored in the physical world, never in screen space
```

반드시 시작 구조→경유 구조→도착 구조, 앞뒤 가림 순서, 카메라 시차, 도착 반응,
퇴장 시각을 이어서 적는다. 4초는 경유 1개, 6초는 2개, 8초는 3~4개가 상한이다.

### Omni Flash 2차 수정

구조와 카메라는 성공했는데 장면 맞춤 3D 그래픽만 실패하면 클립 전체를 재생성하지
않는다. 지원 범위의 구간을 Omni Flash로 편집하고 다음 문장으로 시작한다.

```text
Keep every person, artifact, stone, opening, camera move, lighting change, duration,
and frame timing unchanged. Add only one [scene-specific material and colour] 3D
[graphic function] following [start → via → end], preserving occlusion and parallax.
```

한 번의 수정에서는 설명 그래픽 하나만 바꾼다.

바람·흐름이 구조물 앞에서만 떠다니면 실패다. 기둥 뒤로 가려지고, 굽은 벽을 따라
방향이 바뀌며, 먼지나 천이 반응해야 장면 안에 존재하는 것으로 보인다.

### 장면 연속성별 실패 패턴

| 상황 | 하면 안 되는 것 | 이유 |
|---|---|---|
| **발견 행동** | 한 장면에 타격·발굴·단면·전체 공개를 모두 지시 | 한 시작 이미지로 불가능한 구도 변화가 생김 |
| **단면 공개** | 이미지에 없는 지하 구조를 영상 단계에서 새로 생성 | 구조가 녹거나 위치가 바뀜 |
| **문 개방·진입** | 시작 이미지에 없는 문을 만들거나 문짝을 관통 | 문·경첩·문턱·빈 이동 경로를 이미지에서 먼저 잠금 |
| **추적 카메라** | `zoom in slowly`만 반복 | 즉시 돌리인→핸드헬드 추적→방향 변화→짧은 정착의 속도 곡선을 시간으로 지정 |
| **8~10초 깊은 이동** | 와이드→유물→극단 매크로 또는 전진→후진을 한 번에 지시 | 모델이 5~7초에 동선을 끝낸 뒤 시작 구도로 리셋할 수 있다. 한 공간·한 진행축·한 화면 규모·한 도착점만 허용하고 나머지는 4~6초로 분할 |
| **첫 프레임과 앵커 불일치** | 항공 전경 이미지에 `해안의 소금 결정에서 시작`처럼 보이지 않는 시작점을 지시 | 모델이 첫 1~2초에 새 카메라 위치를 재합성해 순간이동처럼 보인다. 선택된 실제 이미지에서 보이는 물체만 첫 앵커로 사용 |
| **분해·메커니즘** | `assemble` / 새 부품 추가 / 화면 고정 화살표 | 부품이 뭉개지거나 HUD처럼 보임 |
| **미확인 내부** | 구체적인 보물·함정·시신을 확정적으로 생성 | 증거 상태를 왜곡함 |

### 검수 기준 (통과 못 하면 재생성)

- [ ] 선택된 실제 시작 이미지를 열어 `entry_anchor`의 화면 위치·형태를 `start_frame_anchor_evidence`에 기록했는가
- [ ] 4/6/8/10초에 맞춰 2/3/3/4개의 `visual_states`를 영상 프롬프트보다 먼저 잠갔는가
- [ ] 인접한 `visual_states`가 같은 공간·진행축·화면 규모 안에서 물리적으로 이어지는가
- [ ] 시작 프레임의 구조물이 끝까지 같은 물건인가
- [ ] 첫 카메라 앵커가 이미지 프롬프트가 아니라 선택된 실제 시작 이미지에 보이는가
- [ ] 중간에 첫 구도나 시작 이미지로 순간 복귀해 동작을 다시 시작하지 않는가
- [ ] 8~10초 프롬프트에 Start/Mid/Final anchor, 마지막 프레임 유지, cut/reset/loop/restart 금지가 모두 있는가
- [ ] 단면이 중간에 닫히지 않는가
- [ ] 부품 개수가 늘거나 줄지 않는가
- [ ] 카메라가 장면표의 한 행동과 한 목적만 수행하는가
- [ ] 진입점→경유 경로→도착점이 실제 공간으로 이어지고 첫 0.35초 안에 움직이는가
- [ ] 6초 이상 동적 장면에 2~4초 사이 패턴 인터럽트가 있으며 마지막에만 짧게 정착하는가
- [ ] 문·단면 전환에서 카메라가 물체를 관통하거나 확인되지 않은 방을 발명하지 않는가
- [ ] `GENERATED_PHYSICS`가 화면 좌표가 아니라 구조와 재료에 붙어 있는가
- [ ] 흐름이 기둥·벽·유물 앞뒤로 자연스럽게 가려지는가
- [ ] `VEO_INTEGRATED_3D` 그래픽이 실제 시작·경유·도착 구조를 모두 지나는가
- [ ] 카메라 이동·앞뒤 가림이 있는 장면의 `모션 공간`이 `WORLD_3D`인가
- [ ] 그래픽의 형태·재질·색이 장면의 질문과 실제 물성에 맞는가
- [ ] 경로·유선·치수·스캔이 화면 HUD가 아니라 구조물에 가려지고 카메라 시차를 받는가
- [ ] 3D 흐름과 치수 기준점이 붙을 구조 표면이 시작 이미지부터 분명히 보이는가
- [ ] 시작 이미지의 문명·인물·유물·유적 지문이 마지막 프레임까지 유지되는가
- [ ] TTS의 핵심 단어와 카메라·단면·3D 그래픽의 등장 시점이 맞는가
- [ ] 없던 글자가 생기지 않았는가
- [ ] 사람 얼굴이 뭉개져 화면에 크게 잡히지 않는가

---

## 4-4. 가져오기 & 배치

다운로드는 `001, 002, 003…` 순번으로 떨어진다. 내보낸 순서와 같으므로 장면
번호로 되돌려 배치한다.

```
산출물/EP01_진시황릉/
├─ audio/        001.mp3  002.mp3 …   (3단계)
├─ images/       001.png  002.png …   (4단계)
└─ clips/        001.mp4  002.mp4 …   (4단계)
```

**파일명은 3자리 장면 번호로 통일한다.** 5단계 속도 계산기가 이 규칙으로 짝을
맞춘다.

---

## 4-5. 장면 1:1 잠금

승인된 모든 장면은 시작 이미지 1개와 I2V 클립 1개를 가진다. 정지 관찰이 필요한
장면도 I2V에서 미세한 빛·먼지·초점 변화만 주고 카메라를 억지로 움직이지 않는다.

생성 실패나 비용 문제로 이미지를 그대로 써야 할 경우에는 예외를 장면표에 기록하고,
편집 단계에서는 안정된 미세 패닝만 적용한다. 예외가 전체 장면 수나 번호를 바꾸지는 않는다.

---

## 4-6. FLOW 조작 함정 (EP01 실측)

브라우저를 직접 몰아 본 결과 걸린 것들. 이대로 하면 안 걸린다.

| # | 함정 | 대응 |
|---|---|---|
| 1 | 비율을 9:16으로 고르고 **Esc를 누르면 선택이 취소**된다. 16:9로 되돌아가 있다 | 패널은 **입력창을 클릭해서** 닫는다. Esc 금지 |
| 2 | 영상이 하나 완성될 때마다 **이미지 필터가 풀려** 타일 위치가 밀린다. 엉뚱한 이미지가 붙는다 | 그리드에서 고르지 말 것. **`시작` → 에셋 검색**으로 특정한다. 검색어는 `02a.장면구분.json` 의 `search` 필드 |
| 3 | 크롬이 연속 다운로드를 차단한다 | 사이트 권한에서 자동 다운로드를 허용해 둔다 |
| 4 | 다운로드가 다른 크롬 프로필 폴더로 떨어진다 | EP01 기준 **Profile 18** (`셀팜/그록영상(15)`) |
| 5 | 필름스트립 총개수만 보고 완료 처리했더니 한 장면이 누락됐다 | 장면표 ID와 생성 영상 ID를 **1:1 대조**하고 길이 지문도 확인한다 |
| 6 | 같은 장면을 이미지 두 장에서 각각 뽑아 중복 생성 | 장면 하나당 최종 이미지를 **정확히 1장으로 잠근 뒤** I2V에 들어간다 |

### 속도

봇으로 제재당하지 않게 **1개 완성 → 다운로드 → 다음 씬 생성** 순서로 간다.
여러 개를 한꺼번에 큐에 넣지 않는다. 영상 1컷당 약 60초, 이미지 1컷당 30~45초.

### 크레딧 실측 예시 (구버전 EP01, 25컷)

| 항목 | 규격 | 크레딧 |
|---|---|---|
| 이미지 | Nano Banana 2 · 9:16 · x2 | **0** |
| 영상 | Omni Flash · 720p · x1 · 8초 | 12 / 컷 |
| 25컷 총계 | | 약 **300** — 새 회차는 실제 장면 수와 선택 길이로 다시 계산 |


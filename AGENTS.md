# 고대유물의 비밀 — 작업 시작 규칙

이 파일은 저장소 전체에 적용된다.

## “제작 시작”, “다음 주제”, “계속” 요청을 받았을 때

파일 생성·TTS 호출·브라우저 조작보다 먼저 아래를 수행한다.

1. `git status --short`, 현재 브랜치, `HEAD`와 `origin/main`을 확인한다.
2. 작업 트리가 더러우면 사용자 변경을 보존하고 어떤 회차의 어느 단계인지 판별한다.
3. [`00.제작_시작_워크플로우.md`](./00.제작_시작_워크플로우.md)를 처음부터 읽는다.
4. [`00.함정목록.md`](./00.함정목록.md), `채널설정.json`, 현재 단계의 01~07 지침을 읽는다.
5. 에피소드 폴더의 잠금·검수·진행 파일을 확인하고 마지막으로 통과한 게이트 다음부터 시작한다.

채팅 기억이나 화면에 보이는 일부 결과만으로 단계를 건너뛰지 않는다. GitHub 저장소의
현재 워크플로와 잠금 파일이 제작 순서의 기준이다.

## 필수 게이트

- 조사와 증거 지도가 없는 대본 작성 금지.
- `01.문맥검수.json` version 3이 현재 `01.대본.txt`의 SHA-256과 일치하고,
  `korean-proofreader` 문서→문단→문장 교정, 모든 인접 문장 쌍, 정보 선행조건,
  주요 사실 미니후킹, 결론 회수 검문을 모두 통과하기 전 TTS 생성 금지.
- `tools/script_context_gate.py`는 TTS 생성 전에 반드시 실행한다.
- 이미지·영상 프롬프트는 실제 TTS 길이와 고증 카드가 잠긴 뒤 작성한다.
- Flow/Veo 클립은 문명·유물 지문과 월드 통합 3D 설명모션을 장면별로 검수한다.
- 게시용 최종본은 CapCut 템플릿 자막과 마감 효과를 실제 화면에서 확인한 뒤 내보낸다.
  Remotion·ffmpeg 렌더는 프리뷰/복구본이며, 사용자가 명시적으로 승인하지 않으면
  게시 마스터로 취급하지 않는다.
- CapCut 마감 단계에서는 반드시 `skills/ancient-artifact-capcut-finish/SKILL.md`를 읽는다.
  사용자 수동 편집을 보존하고, 노멀라이즈만 적용하며, 4K 게시본을 검수·잠근다.
- `05.캡컷마감잠금.json`이 현재 영상 해시와 일치하기 전 업로드 금지.

## 완료 보고

“완료”라고 보고할 때는 산출물 경로, 검수 결과, CapCut 마감 여부, Git 커밋과
원격 푸시 여부를 구분해 적는다. 하지 않은 단계는 완료로 부르지 않는다.

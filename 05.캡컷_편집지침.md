# 5단계 — 캡컷 편집

장면별 클립 길이를 TTS에 맞추고 자막을 싱크시킨다. 3D 설명모션은 이미
Veo/Omni 클립 안에 통합되어 있어야 하며 편집 단계에서 새로 덧그리지 않는다.
무협 TTS 파이프라인에서 쓰던 "영상을 오디오 길이에 맞춘다"는 원칙과 동일하다.

**게시 마스터는 CapCut에서 실제 프로젝트를 열어 전편을 확인하고 내보낸 파일뿐이다.**
Remotion/ffmpeg 결과는 자막 싱크 프리뷰와 비상 복구용이며 최종본을 대신하지 않는다.

---

## 5-1. 먼저 속도표를 뽑는다

손으로 계산하지 않는다. 3단계 `durations.json`과 4단계 `clips/`를 대조해서
장면별 배속을 계산해 주는 도구가 있다.

```bash
python tools/speed_table.py "산출물/EP01_진시황릉"
```

```
    장면 종류      클립     나레이션      배속   판정  비고
     1 영상      8.00s     5.24s   1.527x   OK
     2 영상      5.00s     6.50s   0.769x   OK
 ×   3 영상      3.00s     9.80s   0.306x   위험  클립이 너무 짧음 — 길게 재생성
```

**계산식**

```
캡컷 배속 = 클립 길이 ÷ 나레이션 길이
```

| 배속 | 판정 | 처리 |
|---|---|---|
| 0.75 ~ 3.00 | OK | 그대로 적용 |
| 0.50~0.75 / 3.00~4.50 | 주의 | 티가 난다. 앞뒤를 잘라 붙이는 편이 낫다 |
| 그 밖 | **위험** | ★ 배속으로 때우지 말고 클립을 다시 뽑는다 |

실제 투탕카멘 편에서 쓴 배속은 **0.78x ~ 3.80x**였다. 생성 클립이 대부분
느린 카메라 무빙이라 3배속을 걸어도 부자연스럽지 않다. 위 범위는 그 실측에 맞춘 것이다.

`--csv`를 붙이면 엑셀로 열 수 있는 표도 같이 나온다.

---

## 5-1b. ★ 타임라인을 손으로 짜지 않는다

타임라인은 손으로 반복 조립하지 않고 **드래프트를 생성한다.** 검증된 기존 프로젝트
("투탕카멘_고대유물의 비밀")를 템플릿으로 삼아 `draft_content.json` 을 다시 쓰는
방식이라, 자막 스타일·워터마크·캔버스 설정이 그대로 따라온다.

```bash
python tools/align_subtitles.py "산출물/EP01_진시황릉"
```
```bash
python tools/capcut_build.py "산출물/EP01_진시황릉" --check
```
```bash
python tools/capcut_build.py "산출물/EP01_진시황릉" --name "EP01 진시황릉"
```

### ★ 자막 싱크는 반드시 align_subtitles 를 먼저 돌린다

빌더 혼자서는 자막을 **장면 길이 안에서 글자수 비율로 배분**할 뿐이다.
그건 근사치라 실제 발화와 눈에 띄게 어긋난다.

`align_subtitles.py` 는 **ElevenLabs 강제 정렬(forced alignment)** 로
장면 mp3와 그 장면 원문을 맞춰 <문자 단위 초>를 받아온다.
Whisper 받아쓰기와 달리 원문을 그대로 정렬하므로 오인식이 없다.

```
대본 → subtitle_split(큐 자르기) → align_subtitles(실측 초 부여) → capcut_build
```

산출물은 `자막_싱크.json`. 빌더는 이 파일을 **필수 입력**으로 요구한다.
파일이 없거나 큐 수·순서·텍스트·겹침·0.15초 미만 큐·장면 경계 중 하나라도 틀리면
`--check`부터 실패하며, 글자수 비율 근사 자막으로 절대 내려가지 않는다.

한 장면의 긴 TTS에서 단어가 씹혔으면 대본이나 자막을 고치지 않는다. 승인 원문을
문장 경계로 나눠 각각 TTS를 다시 만든 뒤 이어 붙이고, 그 장면의 정렬 캐시를 지운 다음
전체 강제 정렬을 다시 실행한다.

```bash
python tools/tts_repair_scene.py "산출물/EPXX_주제/01.대본.txt" \
  --scene 5 --split-after "첫 문장 끝." --run
python tools/align_subtitles.py "산출물/EPXX_주제" --force
python tools/capcut_build.py "산출물/EPXX_주제" --check
```

EP01 실측: 110큐 전부 배치, 순서 역전·겹침 0,
프레임 스냅 오차 최대 17ms(한 프레임 33ms 이내), 표시속도 중앙 11.1자/초.

빌더가 하는 일:

| 항목 | 처리 |
|---|---|
| 영상 | `clips/NNN.*` 를 TTS 길이에 맞춰 배속 계산해 배치 |
| 오디오 | `audio/NNN.mp3` 를 틈 없이 이어 붙임 |
| 자막 | 검증된 `자막_싱크.json`의 ElevenLabs 실측 초만 사용 |
| 워터마크 | `자산워터마크.png` 를 전 구간에 (위치·크로마키 템플릿 유지) |
| 프레임 정렬 | 모든 시간을 30fps 격자에 맞춤 |

### ★ 오디오 원본 보존 — 노멀라이즈만 적용한다

- 영상 원음과 ElevenLabs 나레이션 모두 **음량 노멀라이제이션만** 켠다. 목표는 −23 LUFS다.
- **음성 보정·노이즈 제거·보컬 분리·오디오 효과는 전부 끈다.**
- CapCut 볼륨은 영상 원음 **−15dB**, 나레이션 **+5dB**로 둔다.
- Omni/Veo 영상에 원래 들어 있는 문·발걸음·바람·충격음은 버리지 않는다. 영상 트랙
  원음을 **−15dB** 베드로 살리고 나레이션을 가리면 장면별로만 더 낮춘다.
- 내보내기 전 CapCut을 완전히 종료하고 아래 가드를 실행한다. 이 도구는 자막 `페이드 인`,
  영상 `줌 1`, 장면 전환 `왼쪽으로 밀기`, 자막 `페이드 인`은 보존하면서
  음성 AI 소재·대기 목록만 제거한다.

```powershell
python tools/capcut_audio_guard.py "<CapCut 프로젝트>/draft_content.json" --write
python tools/capcut_audio_guard.py "<CapCut 프로젝트>/draft_content.json"
```

두 번째 명령이 `"passed": true`가 아니면 CapCut을 다시 열거나 내보내지 않는다.

`--check` 는 준비물만 점검하고 끝난다. 빠진 장면 번호를 알려주므로
4단계가 다 끝났는지 확인할 때 쓴다.

생성 후 **CapCut을 실제로 열면** 목록에 나타난다. 다음 항목을 GUI에서 눈과 귀로
확인하고 필요한 편집 타격감을 적용한 뒤 내보낸다. 빌더는 타임라인만 짜며 검수·마감·
내보내기를 대신하지 않는다.

- KCC간판체, 크기 12, 검정 획 0.08, y=−0.206, 입장 `페이드 인` 0.25초
- 이미 캡컷에서 영상 조합 애니메이션을 적용한 드래프트는 재생성하지 않는다. 캡컷 **본체까지 완전히 종료**하고 `tools/capcut_caption_fade_apply.py <draft_content.json> --write`로 text 트랙만 교체한다. 이 도구는 CapCut 복구 사본(`draft_content.json.bak`, `template-2.tmp`)도 함께 바꿔 `줌 1`을 보존한다.
- **효과 적용 순서 고정:** 모든 영상 장면에 필요한 `줌 1`을 먼저 적용하고 저장한다.
  그 뒤 장면 연결이 필요한 경계에만 `왼쪽으로 밀기`를 적용한다. 전환을 먼저 넣은 뒤
  애니메이션을 추가하면 CapCut에서 전환 효과가 씹힐 수 있으므로 역순 작업은 금지한다.
- **효과 겹침 금지:** 한 클립에 `줌 1`과 `왼쪽으로 밀기`를 함께 붙이지 않는다.
  CapCut이 프로젝트를 다시 열거나 자동 저장할 때 같은 클립의 전환 참조를 애니메이션
  앞으로 재정렬해 `줌 1`이 씹힐 수 있다. 전환이 필요한 클립은 전환만 유지한다.
- 영상 조합 애니메이션은 `줌 1`만 허용한다. `반동 1` 등 다른 애니메이션은 금지한다.
- 장면 전환은 `왼쪽으로 밀기`만 허용한다. `페이크 줌` 등 다른 전환은 금지한다.
- 강제 정렬 자막이 실제 발음과 맞고 장면 경계에서 조기 종료되지 않는지
- 처음부터 끝까지 끊지 않고 재생하며 초반·중반·후반에 누적 드리프트가 없는지
- 증거 공개·문 개방·단면 진입처럼 필요한 순간에만 짧은 크래시 줌·흔들림·임팩트가 있는지
- 전체 재생에서 검은 프레임, 무음, 잘린 자막, 빠진 클립이 없는지

### ★ 드래프트 폴더는 3개 파일만 — 템플릿을 통째로 복사하면 안 된다

첫 시도에서 템플릿 폴더를 `copytree` 로 복사했더니 **캡컷이 프로젝트를 열지
못했다.** `draft.extra` · `key_value.json` · `draft_virtual_store.json` ·
`Timelines/` · `subdraft/` 같은 <옛 프로젝트 상태 파일>이 함께 딸려와
새 `draft_content.json` 과 충돌하기 때문이다. 표지도 템플릿 것이 남아
목록에 투탕카멘 썸네일이 떴다.

올바른 구조 — 빈 폴더에 아래 3개만 쓴다:

```
com.lveditor.draft/<프로젝트명>/
├─ draft_content.json     타임라인
├─ draft_meta_info.json   메타 + 미디어 등록부
└─ draft_cover.jpg        첫 클립에서 뽑은 표지
```

함께 챙겨야 할 것 (빠뜨리면 소재를 못 잡거나 안 열린다):
- `draft_content.json` 의 `group_container` 를 `None` 으로
- `draft_meta_info.json` 의 **`draft_materials`** 등록부를 실제 파일로 새로 채움
- `draft_timeline_materials_size_` 재계산
- 모든 소재 경로는 **절대경로** (상대경로면 캡컷이 못 찾는다)

무협 파이프라인의 `capcut_export.py` 가 쓰는 방식과 동일하다.

---

## 5-2. 타임라인 구성 (수동으로 할 경우)

1. **오디오 먼저 깐다.** `audio/001.mp3`부터 순서대로 빈틈없이 이어 붙인다.
   → 이게 타임라인의 기준선이다. 오디오는 절대 늘이거나 줄이지 않는다.
2. 각 오디오 클립의 **시작·끝 지점을 마커로 찍는다.**
3. 대응하는 영상을 그 위에 올린다.
4. 속도표의 배속을 적용해 영상 끝을 마커에 맞춘다.
   - 캡컷: 클립 선택 → **속도 → 일반 → 배속 입력**
   - 0.75x 이하로 느리게 할 때만 **부드러운 슬로우(프레임 보간)** 를 켠다.
     빠르게 할 때는 끈다. 켜면 뭉갠다.
5. **이미지 장면**은 속도 대신 길이를 직접 입력하고 켄번즈를 얹는다.
   - 줌 강도는 길이에 반비례: 3초 이하 강하게, 8초 이상은 아주 약하게
   - 방향은 장면마다 번갈아 가며 (밀기 / 당기기 / 좌 / 우)

> **오디오를 건드리지 않는 이유** — TTS를 늘리면 톤이 무너지고, 줄이면 발음이
> 뭉개진다. 항상 영상 쪽을 맞춘다.

---

## 5-2b. ★ Veo 통합형 3D 설명모션 검수

설명 그래픽은 편집 렌더 뒤에 평면 도형으로 덧붙이지 않는다. `VEO_INTEGRATED_3D` 장면의
경로·바람·물질 흐름·힘·치수·스캔·단면·분해 그래픽은 장면 의미에 맞는 재질과 색으로
Flow/Veo 클립 자체에 들어 있어야 한다.

```
Flow/Veo 통합형 장면 N개 (장면표와 1:1)
  → 실패 장면만 Omni Flash로 그래픽 단독 수정
  → 첫·중간·가림·도착·끝 키프레임 QA
  → capcut_build.py (TTS·강제정렬 자막·워터마크 타임라인)
  → CapCut GUI에서 자막 스타일·싱크·편집 타격감·전체 재생 확인
  → CapCut 내보내기 + capcut_final_lock.py
  → 게시 마스터
```

### 클립 승인 조건

1. 그래픽이 장면표의 시작·경유·도착 앵커를 정확히 지난다.
2. 카메라가 움직여도 그래픽이 화면에 붙지 않고 같은 월드 위치에 남는다.
3. 기둥·벽·유물 뒤에서 가려지고 다시 나타난다.
4. 구조물의 원근·곡면·높이 변화를 따라간다.
5. 도착점에서 먼지·반사광·진동·초점 이동 중 지정한 반응 하나가 발생한다.
6. 구조물·유물·부품·개구부의 형태와 개수가 끝까지 유지된다.
7. 설명 그래픽이 끝 프레임 전에 자연스럽게 퇴장한다.
8. 텍스트·숫자·라벨·검은 카드·화면 고정 HUD가 생성되지 않는다.
9. 시작 이미지의 문명·인물·유물·유적 형태가 마지막 프레임까지 유지된다.
10. 그래픽과 카메라의 시작·전환·도착이 실제 TTS 구절과 맞는다.

### 장면 맞춤 3D 시각 언어 검수

- 빨강은 공통색이 아니다. 위험·침입·붕괴 경계처럼 의미가 있을 때만 진사·주홍을 쓴다.
- 바람은 옅은 온백색·황토 유선, 수은은 은빛 거울 재질, 힘은 청동·호박색 접촉 펄스,
  치수는 대비되는 중성색, 스캔은 절제된 청록·청백색을 우선한다.
- 장면 질문·증거·실제 물질과 관계없는 색·형태면 실패다.
- 형광 네온·레이저·게임 UI·무지개 다색처럼 보이면 실패다.
- 경로·유선·물질이 벽을 뚫거나 경유 앵커를 건너뛰면 실패다.
- 가까운 구간과 먼 구간의 크기 차이, 카메라 시차, 구조물 가림 중 두 가지 이상이
  프레임에서 확인되어야 한다.
- 정확한 수치는 Veo에 쓰게 하지 않고 TTS와 기존 자막이 전달한다.

### 실패 복구

구조·카메라가 좋은 클립은 버리지 않는다. 최대 10초 구간을 Omni Flash에서 열고
`Keep every stone, opening, camera move, lighting change, duration, and frame timing
unchanged. Add only...`로 그래픽 하나만 수정한다. 같은 수정에서 색보정·카메라·구조를
함께 바꾸지 않는다.

### 키프레임 QA

각 `VEO_INTEGRATED_3D` 클립에서 최소 5장을 추출한다.

1. 그래픽 발생 직전
2. 첫 경유 앵커
3. 구조물 뒤로 가려지는 순간
4. 도착 반응
5. 그래픽 퇴장 뒤

한 장이라도 장면표와 다르면 불합격이다. 자세한 생성 문법은
`02V.VEO_통합3D_설명모션.md`를 따른다.

---

## 5-3. 자막

> 글꼴·크기·획·위치는 실제 캡컷 프로젝트 **"투탕카멘_고대유물의 비밀"** 의
> 자막 111개를 실측한 값이다. 입장 애니메이션만 현재 채널 결정에 따라
> `하나씩` 33ms에서 `페이드 인` 0.25초로 교체했다.

### 고정 스타일

| 항목 | 값 | 캡컷 JSON |
|---|---|---|
| 폰트 | **KCC간판체** | `font_title: KCC간판체` |
| 크기 | **12** | `font_size: 12.0` (`text_size: 30`) |
| 색 | 흰색 | `fill: [1,1,1]` |
| 테두리(획) | **검정, 0.08** | `border_width: 0.08` |
| 그림자 | **없음** | `has_shadow: false` |
| 정렬 | 가운데 | `alignment: 1` |
| 자간 / 행간 | 0 / 0.02 | |
| 줄 최대 폭 | 0.82 | `line_max_width: 0.82` |
| **위치** | **가로 중앙, 세로 −0.206** | `transform: {x: 0, y: -0.206}` |
| 스케일 | 1.0 | |

세로 −0.206은 화면 중앙에서 아래로 약 20% 내린 지점이다. 쇼츠 UI가 먹는
하단 18%보다는 위에 있다.

### 애니메이션

| 항목 | 값 |
|---|---|
| 입장 | **"페이드 인"** |
| 지속 | **0.25초 (250ms)** |
| 퇴장 | **없음** |

모든 자막에 같은 값을 적용한다. 페이드가 낭독을 늦게 따라오지 않도록 0.25초를
넘기지 않고, 퇴장 애니메이션은 쓰지 않는다. `capcut_build.py`가 템플릿의 기존
애니메이션을 그대로 복제하지 않고 이 값을 강제한다.

### ★ 자막 분할 — 한 줄 · 어절 보존

실측 결과 **111개 전부 한 줄이고 줄바꿈이 0개**다. 3~17자, 중앙값 9자.

이 규칙을 손으로 지키기 어려우므로 도구로 강제한다.

```bash
python tools/subtitle_split.py "산출물/EP01_진시황릉/01.대본.txt" --srt
```

| 규칙 | 값 | 근거 |
|---|---|---|
| 한 줄 | 줄바꿈 금지 | 실측 111/111 |
| 상한 | **16자** | 실측 최대 17자에서 1자 여유 |
| 목표 | 9자 안팎 | 실측 중앙값 |
| 경계 | **어절(공백)에서만** | |
| 숫자+단위 분리 금지 | `2천 년을` | "2천 / 년을" 방지 |
| 보조용언 분리 금지 | `갈아 넣었죠` | "갈아 / 넣었죠" 방지 |
| 조사·의존명사 단독 시작 금지 | | "것/겁/수/때…"가 줄 앞에 홀로 오지 않게 |
| 짧은 꼬리 재분배 | 3자 미만 | |

출력은 `자막.json`(큐 목록)과 `--srt` 지정 시 `자막.srt`다.
**SRT의 타이밍은 임시값이다.** 실제 싱크는 캡컷에서 TTS에 맞춘다.
상한을 넘는 큐가 하나라도 나오면 종료 코드 1로 알려준다 — 그때는 배속이 아니라
**대본 문장을 줄인다.**

### 타이밍 (실측)

| 항목 | 값 |
|---|---|
| 자막 1개 길이 | 0.30 ~ 2.17초 (중앙 1.07초) |
| 표시 속도 | 5.3 ~ 13.3자/초 (중앙 8.5) |
| 자막 사이 간격 | 0 ~ 0.30초 (중앙 **0.017초**) |

간격 중앙값이 0.017초라는 건 **자막이 거의 끊김 없이 이어진다**는 뜻이다.
빈 화면을 만들지 않는다.

---

## 5-4. 워터마크 (실측)

| 항목 | 값 |
|---|---|
| 소재 | PNG 1254×1254 |
| 위치 | **x = 0.6772, y = −0.8164** (우하단) |
| 스케일 | **0.2304** |
| 불투명도 | 1.0 |
| 크로마키 | **`#fcfcfc`** — 흰 배경을 빼서 로고만 남김 |
| 구간 | 영상 전체 (0.17초 ~ 끝) |
| 트랙 | 별도 오버레이 트랙 (`flag: 2`) |

> **원본 위치: `자산워터마크.png`** (채널 폴더 루트, 저장소에 포함됨)
> 캡컷 프로젝트는 아직 `C:/Users/7500F/Downloads/exec-….png` 를 보고 있으므로,
> 다음 편집 때 **이 파일로 다시 연결**할 것. 다운로드 폴더는 지워지기 쉽다.

---

## 5-4b. 온스크린 라벨 — 보조용

**영문 라벨과 붉은 화살표는 4단계 이미지에서 이미 만들어져 온다** (02 지침 v2.0).
여기서 얹는 건 두 경우뿐이다.

1. 이미지에서 글자가 깨져 나온 컷의 재작업
2. **한국어** 보조 설명 (이미지 모델은 한글을 못 쓴다)

| 항목 | 값 |
|---|---|
| 폰트 | 영문 산세리프 (Archivo / Helvetica 계열) |
| 색 | 흰색 또는 밝은 적색 |
| 크기 | 자막의 60% |
| 위치 | 해당 부품 옆. 얇은 지시선을 함께 |
| 개수 | 컷당 최대 2개 |

단면·분해·원리 컷(L3~L5)에만. 전경 컷에는 넣지 않는다.

---

## 5-5. 사운드

| 트랙 | 레벨 | 비고 |
|---|---|---|
| 나레이션 | −16 LUFS | 기준 |
| BGM | −26 LUFS | 챕터 전환에서만 살짝 올린다 |
| 효과음 | −22 LUFS | 아래 지점에만 |

효과음은 **아껴야 한다.** 넣는 지점은 셋뿐이다.

1. Ch3 붕괴 순간 — 낮은 임팩트 1회
2. Ch4 단면 진입 순간 — 짧은 스위시 1회
3. Ch4 메커니즘 작동 — 재질에 맞는 소리 1회 (모래/물/금속)

---

## 5-6. 내보내기

| 항목 | 값 |
|---|---|
| 해상도 | **2160×3840 (9:16, 4K)** |
| 프레임 | 30fps |
| 비트레이트 | 권장 이상 |
| 코덱 | H.264 |
| 파일명 | `완성본_EP01_진시황릉_capcut.mp4` |
| 저장 위치 | `산출물/EP01_진시황릉/` |

OneDrive 경로처럼 한글·대괄호·긴 폴더명이 섞인 위치에서는 CapCut이 완료 화면을
보여도 파일이 생성되지 않을 수 있다. 이때 완료 화면을 성공 증거로 보지 않는다.
짧은 로컬 임시 경로(예: `C:\Temp\EP01_capcut.mp4`)로 먼저 내보내고, 파일 존재와
ffprobe의 해상도·fps·길이·오디오 스트림을 확인한 뒤 에피소드 결과 폴더로 복사한다.

### 내보내기 전 확인

- [ ] 총 길이가 **3분 미만**인가 (쇼츠 상한)
- [ ] 하단 18%에 자막·중요 정보가 걸치지 않는가 (쇼츠 UI가 가린다)
- [ ] 첫 3초에 후킹이 완결되는가
- [ ] 마지막 프레임이 검게 끝나지 않는가
- [ ] 오디오가 끊기거나 겹치는 구간이 없는가
- [ ] 모든 영상 원음·TTS는 음량 노멀라이즈만 켜졌고 음성 보정은 꺼졌는가
- [ ] 영상 원음 −15dB, TTS +5dB인가
- [ ] 속도표의 **위험** 장면을 전부 처리했는가
- [ ] CapCut 저장 템플릿의 자막 모양을 실제 미리보기에서 확인했는가
- [ ] 편집 타격감은 필요한 증거 공개 순간에만 들어가고 Veo의 3D 월드 모션을 가리지 않는가
- [ ] 처음부터 끝까지 실제 재생해 검은 프레임·무음·누락이 없는가

내보낸 뒤에는 GUI 확인 사실과 영상 해시를 잠근다. 다섯 플래그는 실제로 확인했을 때만 붙인다.

```powershell
python tools/capcut_final_lock.py "산출물/EP01_진시황릉" --write `
  --video "산출물/EP01_진시황릉/완성본_EP01_진시황릉_capcut.mp4" `
  --project-name "EP01 진시황릉" `
  --draft-content "<CapCut 프로젝트>/draft_content.json" `
  --caption-style-verified --caption-sync-verified `
  --motion-finish-verified --full-playback-verified `
  --audio-policy-verified
```

`05.캡컷마감잠금.json`이 없거나 영상 SHA-256이 바뀌면 `youtube_upload.py`가
점검 모드부터 실패한다. Remotion/ffmpeg 프리뷰를 `--영상`으로 강제 지정해도 통과하지 않는다.

# 5단계 — 캡컷 편집

장면별 클립 길이를 TTS에 맞추고, 자막을 싱크시킨 뒤 Remotion 기술
모션그래픽을 합성해 내보낸다.
무협 TTS 파이프라인에서 쓰던 "영상을 오디오 길이에 맞춘다"는 원칙과 동일하다.

---

## 5-1. 먼저 속도표를 뽑는다

손으로 계산하지 않는다. 3단계 `durations.json`과 4단계 `clips/`를 대조해서
장면별 배속을 계산해 주는 도구가 있다.

```bash
python tools/speed_table.py "산출물/EP01_진시황릉"
```

```
    장면 종류      클립     나레이션      배속   판정  비고
     1 영상      8.00s     5.24s   1.527x   OK
     2 영상      5.00s     6.50s   0.769x   OK
 ×   3 영상      3.00s     9.80s   0.306x   위험  클립이 너무 짧음 — 길게 재생성
```

**계산식**

```
캡컷 배속 = 클립 길이 ÷ 나레이션 길이
```

| 배속 | 판정 | 처리 |
|---|---|---|
| 0.75 ~ 3.00 | OK | 그대로 적용 |
| 0.50~0.75 / 3.00~4.50 | 주의 | 티가 난다. 앞뒤를 잘라 붙이는 편이 낫다 |
| 그 밖 | **위험** | ★ 배속으로 때우지 말고 클립을 다시 뽑는다 |

실제 투탕카멘 편에서 쓴 배속은 **0.78x ~ 3.80x**였다. 생성 클립이 대부분
느린 카메라 무빙이라 3배속을 걸어도 부자연스럽지 않다. 위 범위는 그 실측에 맞춘 것이다.

`--csv`를 붙이면 엑셀로 열 수 있는 표도 같이 나온다.

---

## 5-1b. ★ 타임라인을 손으로 짜지 않는다

캡컷을 직접 조작하는 대신 **드래프트를 생성한다.** 검증된 기존 프로젝트
("투탕카멘_고대유물의 비밀")를 템플릿으로 삼아 `draft_content.json` 을 다시 쓰는
방식이라, 자막 스타일·워터마크·캔버스 설정이 그대로 따라온다.

```bash
python tools/align_subtitles.py "산출물/EP01_진시황릉"
```
```bash
python tools/capcut_build.py "산출물/EP01_진시황릉" --check
```
```bash
python tools/capcut_build.py "산출물/EP01_진시황릉" --name "EP01 진시황릉"
```

### ★ 자막 싱크는 반드시 align_subtitles 를 먼저 돌린다

빌더 혼자서는 자막을 **장면 길이 안에서 글자수 비율로 배분**할 뿐이다.
그건 근사치라 실제 발화와 눈에 띄게 어긋난다.

`align_subtitles.py` 는 **ElevenLabs 강제 정렬(forced alignment)** 로
장면 mp3와 그 장면 원문을 맞춰 <문자 단위 초>를 받아온다.
Whisper 받아쓰기와 달리 원문을 그대로 정렬하므로 오인식이 없다.

```
대본 → subtitle_split(큐 자르기) → align_subtitles(실측 초 부여) → capcut_build
```

산출물은 `자막_싱크.json`. 빌더는 이 파일이 있으면 자동으로 우선 사용하고,
없으면 근사 배분으로 떨어지면서 경고를 찍는다.

EP01 실측: 110큐 전부 배치, 순서 역전·겹침 0,
프레임 스냅 오차 최대 17ms(한 프레임 33ms 이내), 표시속도 중앙 11.1자/초.

빌더가 하는 일:

| 항목 | 처리 |
|---|---|
| 영상 | `clips/NNN.*` 를 TTS 길이에 맞춰 배속 계산해 배치 |
| 오디오 | `audio/NNN.mp3` 를 틈 없이 이어 붙임 |
| 자막 | `자막.json` 큐를 장면 길이 안에서 글자수 비율로 배분 |
| 워터마크 | `자산워터마크.png` 를 전 구간에 (위치·크로마키 템플릿 유지) |
| 프레임 정렬 | 모든 시간을 30fps 격자에 맞춤 |

`--check` 는 준비물만 점검하고 끝난다. 빠진 장면 번호를 알려주므로
4단계가 다 끝났는지 확인할 때 쓴다.

생성 후 **캡컷을 다시 열면** 목록에 나타난다. 배속과 자막 싱크를 눈으로
확인한 뒤 내보낸다. 빌더는 타임라인을 짜줄 뿐 검수를 대신하지 않는다.

### ★ 드래프트 폴더는 3개 파일만 — 템플릿을 통째로 복사하면 안 된다

첫 시도에서 템플릿 폴더를 `copytree` 로 복사했더니 **캡컷이 프로젝트를 열지
못했다.** `draft.extra` · `key_value.json` · `draft_virtual_store.json` ·
`Timelines/` · `subdraft/` 같은 <옛 프로젝트 상태 파일>이 함께 딸려와
새 `draft_content.json` 과 충돌하기 때문이다. 표지도 템플릿 것이 남아
목록에 투탕카멘 썸네일이 떴다.

올바른 구조 — 빈 폴더에 아래 3개만 쓴다:

```
com.lveditor.draft/<프로젝트명>/
├─ draft_content.json     타임라인
├─ draft_meta_info.json   메타 + 미디어 등록부
└─ draft_cover.jpg        첫 클립에서 뽑은 표지
```

함께 챙겨야 할 것 (빠뜨리면 소재를 못 잡거나 안 열린다):
- `draft_content.json` 의 `group_container` 를 `None` 으로
- `draft_meta_info.json` 의 **`draft_materials`** 등록부를 실제 파일로 새로 채움
- `draft_timeline_materials_size_` 재계산
- 모든 소재 경로는 **절대경로** (상대경로면 캡컷이 못 찾는다)

무협 파이프라인의 `capcut_export.py` 가 쓰는 방식과 동일하다.

---

## 5-2. 타임라인 구성 (수동으로 할 경우)

1. **오디오 먼저 깐다.** `audio/001.mp3`부터 순서대로 빈틈없이 이어 붙인다.
   → 이게 타임라인의 기준선이다. 오디오는 절대 늘이거나 줄이지 않는다.
2. 각 오디오 클립의 **시작·끝 지점을 마커로 찍는다.**
3. 대응하는 영상을 그 위에 올린다.
4. 속도표의 배속을 적용해 영상 끝을 마커에 맞춘다.
   - 캡컷: 클립 선택 → **속도 → 일반 → 배속 입력**
   - 0.75x 이하로 느리게 할 때만 **부드러운 슬로우(프레임 보간)** 를 켠다.
     빠르게 할 때는 끈다. 켜면 뭉갠다.
5. **이미지 장면**은 속도 대신 길이를 직접 입력하고 켄번즈를 얹는다.
   - 줌 강도는 길이에 반비례: 3초 이하 강하게, 8초 이상은 아주 약하게
   - 방향은 장면마다 번갈아 가며 (밀기 / 당기기 / 좌 / 우)

> **오디오를 건드리지 않는 이유** — TTS를 늘리면 톤이 무너지고, 줄이면 발음이
> 뭉개진다. 항상 영상 쪽을 맞춘다.

---

## 5-2b. ★ Remotion 피사체 결합형 모션그래픽 (필수 선별 적용)

`render_final.py` 로 자막·워터마크·오디오까지 들어간 기본 영상을 먼저 굽고,
그 위에 Remotion으로 설명 그래픽을 합성한다. **TTS와 자막 타임라인은 다시 만들지
않는다.** 모션은 장면의 이해를 돕는 구간에만 넣고, 모든 컷을 장식하지 않는다.

Remotion은 화면 위에 제목판을 올리는 도구가 아니다. 선·입자·치수가 영상 속 유물과
공간에 붙어 움직이도록 합성하는 도구다. 장면표의 `TRACKED_COMPOSITE`와
`INFO_OVERLAY`만 담당하며, `GENERATED_PHYSICS`를 다시 덧그리지 않는다.

```
Flow 승인 장면 N개 (장면표와 1:1)
  → render_final.py (TTS·자막 기준의 기본 합성)
  → Remotion (추적 흐름·치수선·지도 경로·증거 배지·스캔파)
  → 키프레임 시각 검수
  → 최종 mp4
```

### 장면 유형별 필수 그래픽

| 장면 | 적용할 그래픽 | 금지 |
|---|---|---|
| 외형·규모 | 양방향 치수선, 높이·폭 수치 | 근거 없는 정밀 수치 |
| 지하·절개 | 깊이선, 층·통로 라벨, 단면 강조 | 화면과 무관한 지도선 |
| 발견 행동 | 접촉점 확대, 짧은 충격 링, 다음 단면 장면으로 이어지는 초점선 | 행동보다 먼저 답을 노출하는 그래픽 |
| 유물 매크로 | 재료·손상·제작 흔적 지시선, 근거 있는 치수 | 유물에 없는 부품명 |
| 공간 지도 | 발견지→부속 구역→중심부 경로, 발굴/미발굴 구역 분리 | 측정되지 않은 내부 확정선 |
| 기계 장치 | 실제로 확인된 부품명, 회전축, 힘의 방향, 작동 순서 | 영상 속 부품과 어긋난 지시선 |
| 문헌·학술 재구성 | `문헌 기록`·`학술 재구성` 배지, 반투명 도식 | 발굴 실물처럼 보이는 불투명 재현 |
| 물질·환경 | 입자, 흐름, 농도·층 표시 | 과도한 HUD·게임 UI |
| 조사·보존 | 스캔선, 관측 범위, 비파괴 조사 표기 | 실제로 촬영한 듯한 단정 |

### 촌스러운 HUD를 막는 절대 규칙

- 큰 영문 헤더와 한글 제목을 화면 상단에 고정하지 않는다.
- 검은 유리 카드 안에 숫자·설명을 오래 띄우지 않는다.
- 선과 라벨을 화면 고정 좌표로 두지 않는다.
- 네온 화살표 하나가 한 번 그려지고 멈추는 표현을 금지한다.
- 모든 요소를 동시에 페이드인하지 않는다.
- 같은 색의 발광 요소를 한 프레임에 둘 이상 두지 않는다.

### 흐름 모션 문법

바람길·물길·에너지·이동 경로는 다음 순서로 만든다.

1. 실제 입구 또는 접촉점에서 0.2~0.4초의 작은 기점이 생긴다.
2. 얇은 경로가 대상의 곡면·통로를 따라 일부만 드러난다.
3. 3~6프레임 차이로 짧은 꼬리를 가진 펄스 3~7개가 경로를 이동한다.
4. 펄스는 기둥·벽·유물 뒤에서는 마스크로 가리고 앞에서는 다시 나타난다.
5. 도착점에서 먼지·광량·진동 같은 작은 반응이 한 번 발생한다.
6. 나레이션이 끝나기 8~12프레임 전에 흐름이 먼저 빠르게 사라진다.

카메라가 움직이는 장면은 시작·중간·끝의 좌표를 추적해 경로 전체를 보정한다. 단순
좌우 패닝도 최소 3개 키프레임을 둔다. 좌표를 잡을 수 없는 심한 카메라 이동은 억지로
Remotion 합성을 하지 말고 I2V의 `GENERATED_PHYSICS`로 넘긴다.

### 치수선 문법

- 화면이 안정된 뒤 대상 표면의 두 기준점에서 짧은 틱이 먼저 자란다.
- 두 틱 사이를 1~2px 선이 연결하며, 숫자는 선 중앙이 아니라 빈 공간 쪽에 작게 붙는다.
- 피사체가 움직이면 두 끝점과 숫자 위치가 함께 추적된다.
- 수치는 1.0~1.8초만 충분히 읽히고 빠르게 퇴장한다.
- 배경 박스는 원칙적으로 없고, 대비가 부족할 때만 얇은 반투명 스트립을 쓴다.

### 레이어와 깊이

한 장면을 최소 다음 순서로 구성한다.

```
배경 영상
→ 구조물 뒤로 들어가는 흐름
→ 중경 유물·인물
→ 구조물 앞으로 나오는 흐름과 치수선
→ 최소 정보 배지
→ 색보정
→ 그레인·비네트
```

전경 마스크가 없으면 흐름이 영상 위에 떠 보인다. 바람길·통로·힘 경로 장면에서는
마스크 또는 깊이 분리가 필수다.

### 타이밍·움직임

- 입장은 opacity·이동·scale 가운데 2~3개를 함께 사용한다.
- 선·입자·단어는 3~6프레임씩 시차를 둔다.
- 모든 보간은 easing과 좌우 clamp를 쓴다. 선형 이동을 금지한다.
- 2초 이상 머무는 요소는 1~2%의 미세한 호흡만 준다.
- HIT → 15~20프레임 정지 → BUILD → HIT 리듬을 사용한다.
- 큰 이동과 장면 전환에는 나레이션을 해치지 않는 짧은 whoosh·공기음을 2~3프레임 먼저 둔다.

표시는 `장면구분.json`의 실제 시작·끝 시간에 고정한다. 화면 하단 자막 안전영역과
우측 상단 워터마크를 피하고, 치수선의 양 끝은 **영상 속 실제 대상**을 끝까지 추적해야 한다.
수치가 문헌상 추정이면 `약`, 복원·가상 설명이면 `문헌 기록` 또는 `학술 재구성`,
열리지 않은 곳은 `내부 미확인`을 함께 표시한다.

### 검수 기준

1. 모션 대상 장면마다 중간 키프레임을 이미지로 추출해 눈으로 확인한다.
2. 지시선 끝점·라벨·수치가 대상과 맞는지 확인한다.
3. 자막·워터마크와 겹치거나 화면 밖으로 잘리는 요소가 없어야 한다.
4. 입장과 퇴장 애니메이션을 모두 둔다. 선형 이징과 갑작스러운 팝업은 금지한다.
5. 흐름 경로가 전경 구조물 뒤로 가려졌다가 다시 나오는지 확인한다.
6. 첫·중간·끝 키프레임에서 지시선 끝점이 같은 피사체에 붙어 있는지 확인한다.
7. 큰 제목판·검은 카드·화면 고정 HUD가 남아 있으면 불합격이다.
5. 최종 렌더에서 다시 같은 시각의 프레임을 추출해 2차 검수한다.

---

## 5-3. 자막

> 아래 값은 전부 실제 캡컷 프로젝트 **"투탕카멘_고대유물의 비밀"** 의 자막 111개를
> 실측한 것이다. 추정치가 아니다.

### 고정 스타일

| 항목 | 값 | 캡컷 JSON |
|---|---|---|
| 폰트 | **KCC간판체** | `font_title: KCC간판체` |
| 크기 | **12** | `font_size: 12.0` (`text_size: 30`) |
| 색 | 흰색 | `fill: [1,1,1]` |
| 테두리(획) | **검정, 0.08** | `border_width: 0.08` |
| 그림자 | **없음** | `has_shadow: false` |
| 정렬 | 가운데 | `alignment: 1` |
| 자간 / 행간 | 0 / 0.02 | |
| 줄 최대 폭 | 0.82 | `line_max_width: 0.82` |
| **위치** | **가로 중앙, 세로 −0.206** | `transform: {x: 0, y: -0.206}` |
| 스케일 | 1.0 | |

세로 −0.206은 화면 중앙에서 아래로 약 20% 내린 지점이다. 쇼츠 UI가 먹는
하단 18%보다는 위에 있다.

### 애니메이션

| 항목 | 값 |
|---|---|
| 입장 | **"하나씩"** |
| 지속 | **33ms** (111개 중 100개. 나머지는 500ms·36ms) |
| 퇴장 | **없음** |

33ms는 사실상 즉시 나타나는 값이다. 글자가 또르륵 흐르는 연출이 아니라
**딱딱 끊어 찍히는** 느낌이라는 뜻이다. 프리셋으로 저장해 두고 매번 불러 쓴다.

### ★ 자막 분할 — 한 줄 · 어절 보존

실측 결과 **111개 전부 한 줄이고 줄바꿈이 0개**다. 3~17자, 중앙값 9자.

이 규칙을 손으로 지키기 어려우므로 도구로 강제한다.

```bash
python tools/subtitle_split.py "산출물/EP01_진시황릉/01.대본.txt" --srt
```

| 규칙 | 값 | 근거 |
|---|---|---|
| 한 줄 | 줄바꿈 금지 | 실측 111/111 |
| 상한 | **16자** | 실측 최대 17자에서 1자 여유 |
| 목표 | 9자 안팎 | 실측 중앙값 |
| 경계 | **어절(공백)에서만** | |
| 숫자+단위 분리 금지 | `2천 년을` | "2천 / 년을" 방지 |
| 보조용언 분리 금지 | `갈아 넣었죠` | "갈아 / 넣었죠" 방지 |
| 조사·의존명사 단독 시작 금지 | | "것/겁/수/때…"가 줄 앞에 홀로 오지 않게 |
| 짧은 꼬리 재분배 | 3자 미만 | |

출력은 `자막.json`(큐 목록)과 `--srt` 지정 시 `자막.srt`다.
**SRT의 타이밍은 임시값이다.** 실제 싱크는 캡컷에서 TTS에 맞춘다.
상한을 넘는 큐가 하나라도 나오면 종료 코드 1로 알려준다 — 그때는 배속이 아니라
**대본 문장을 줄인다.**

### 타이밍 (실측)

| 항목 | 값 |
|---|---|
| 자막 1개 길이 | 0.30 ~ 2.17초 (중앙 1.07초) |
| 표시 속도 | 5.3 ~ 13.3자/초 (중앙 8.5) |
| 자막 사이 간격 | 0 ~ 0.30초 (중앙 **0.017초**) |

간격 중앙값이 0.017초라는 건 **자막이 거의 끊김 없이 이어진다**는 뜻이다.
빈 화면을 만들지 않는다.

---

## 5-4. 워터마크 (실측)

| 항목 | 값 |
|---|---|
| 소재 | PNG 1254×1254 |
| 위치 | **x = 0.6772, y = −0.8164** (우하단) |
| 스케일 | **0.2304** |
| 불투명도 | 1.0 |
| 크로마키 | **`#fcfcfc`** — 흰 배경을 빼서 로고만 남김 |
| 구간 | 영상 전체 (0.17초 ~ 끝) |
| 트랙 | 별도 오버레이 트랙 (`flag: 2`) |

> **원본 위치: `자산워터마크.png`** (채널 폴더 루트, 저장소에 포함됨)
> 캡컷 프로젝트는 아직 `C:/Users/7500F/Downloads/exec-….png` 를 보고 있으므로,
> 다음 편집 때 **이 파일로 다시 연결**할 것. 다운로드 폴더는 지워지기 쉽다.

---

## 5-4b. 온스크린 라벨 — 보조용

**영문 라벨과 붉은 화살표는 4단계 이미지에서 이미 만들어져 온다** (02 지침 v2.0).
여기서 얹는 건 두 경우뿐이다.

1. 이미지에서 글자가 깨져 나온 컷의 재작업
2. **한국어** 보조 설명 (이미지 모델은 한글을 못 쓴다)

| 항목 | 값 |
|---|---|
| 폰트 | 영문 산세리프 (Archivo / Helvetica 계열) |
| 색 | 흰색 또는 밝은 적색 |
| 크기 | 자막의 60% |
| 위치 | 해당 부품 옆. 얇은 지시선을 함께 |
| 개수 | 컷당 최대 2개 |

단면·분해·원리 컷(L3~L5)에만. 전경 컷에는 넣지 않는다.

---

## 5-5. 사운드

| 트랙 | 레벨 | 비고 |
|---|---|---|
| 나레이션 | −16 LUFS | 기준 |
| BGM | −26 LUFS | 챕터 전환에서만 살짝 올린다 |
| 효과음 | −22 LUFS | 아래 지점에만 |

효과음은 **아껴야 한다.** 넣는 지점은 셋뿐이다.

1. Ch3 붕괴 순간 — 낮은 임팩트 1회
2. Ch4 단면 진입 순간 — 짧은 스위시 1회
3. Ch4 메커니즘 작동 — 재질에 맞는 소리 1회 (모래/물/금속)

---

## 5-6. 내보내기

| 항목 | 값 |
|---|---|
| 해상도 | **1080×1920 (9:16)** |
| 프레임 | 30fps |
| 비트레이트 | 권장 이상 |
| 코덱 | H.264 |
| 파일명 | `EP01_진시황릉_final.mp4` |
| 저장 위치 | `산출물/EP01_진시황릉/` |

### 내보내기 전 확인

- [ ] 총 길이가 **3분 미만**인가 (쇼츠 상한)
- [ ] 하단 18%에 자막·중요 정보가 걸치지 않는가 (쇼츠 UI가 가린다)
- [ ] 첫 3초에 후킹이 완결되는가
- [ ] 마지막 프레임이 검게 끝나지 않는가
- [ ] 오디오가 끊기거나 겹치는 구간이 없는가
- [ ] 속도표의 **위험** 장면을 전부 처리했는가

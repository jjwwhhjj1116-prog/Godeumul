# 5단계 — 캡컷 편집

장면별 클립 길이를 TTS에 맞추고, 자막을 싱크시켜 내보낸다.
무협 TTS 파이프라인에서 쓰던 "영상을 오디오 길이에 맞춘다"는 원칙과 동일하다.

---

## 5-1. 먼저 속도표를 뽑는다

손으로 계산하지 않는다. 3단계 `durations.json`과 4단계 `clips/`를 대조해서
장면별 배속을 계산해 주는 도구가 있다.

```bash
python tools/speed_table.py "산출물/EP01_진시황릉"
```

```
    장면 종류      클립     나레이션      배속   판정  비고
     1 영상      8.00s     5.24s   1.527x   OK
     2 영상      5.00s     6.50s   0.769x   OK
 ×   3 영상      3.00s     9.80s   0.306x   위험  클립이 너무 짧음 — 길게 재생성
```

**계산식**

```
캡컷 배속 = 클립 길이 ÷ 나레이션 길이
```

| 배속 | 판정 | 처리 |
|---|---|---|
| 0.75 ~ 3.00 | OK | 그대로 적용 |
| 0.50~0.75 / 3.00~4.50 | 주의 | 티가 난다. 앞뒤를 잘라 붙이는 편이 낫다 |
| 그 밖 | **위험** | ★ 배속으로 때우지 말고 클립을 다시 뽑는다 |

실제 투탕카멘 편에서 쓴 배속은 **0.78x ~ 3.80x**였다. 생성 클립이 대부분
느린 카메라 무빙이라 3배속을 걸어도 부자연스럽지 않다. 위 범위는 그 실측에 맞춘 것이다.

`--csv`를 붙이면 엑셀로 열 수 있는 표도 같이 나온다.

---

## 5-2. 타임라인 구성

1. **오디오 먼저 깐다.** `audio/001.mp3`부터 순서대로 빈틈없이 이어 붙인다.
   → 이게 타임라인의 기준선이다. 오디오는 절대 늘이거나 줄이지 않는다.
2. 각 오디오 클립의 **시작·끝 지점을 마커로 찍는다.**
3. 대응하는 영상을 그 위에 올린다.
4. 속도표의 배속을 적용해 영상 끝을 마커에 맞춘다.
   - 캡컷: 클립 선택 → **속도 → 일반 → 배속 입력**
   - 0.75x 이하로 느리게 할 때만 **부드러운 슬로우(프레임 보간)** 를 켠다.
     빠르게 할 때는 끈다. 켜면 뭉갠다.
5. **이미지 장면**은 속도 대신 길이를 직접 입력하고 켄번즈를 얹는다.
   - 줌 강도는 길이에 반비례: 3초 이하 강하게, 8초 이상은 아주 약하게
   - 방향은 장면마다 번갈아 가며 (밀기 / 당기기 / 좌 / 우)

> **오디오를 건드리지 않는 이유** — TTS를 늘리면 톤이 무너지고, 줄이면 발음이
> 뭉개진다. 항상 영상 쪽을 맞춘다.

---

## 5-3. 자막

> 아래 값은 전부 실제 캡컷 프로젝트 **"투탕카멘_고대유물의 비밀"** 의 자막 111개를
> 실측한 것이다. 추정치가 아니다.

### 고정 스타일

| 항목 | 값 | 캡컷 JSON |
|---|---|---|
| 폰트 | **KCC간판체** | `font_title: KCC간판체` |
| 크기 | **12** | `font_size: 12.0` (`text_size: 30`) |
| 색 | 흰색 | `fill: [1,1,1]` |
| 테두리(획) | **검정, 0.08** | `border_width: 0.08` |
| 그림자 | **없음** | `has_shadow: false` |
| 정렬 | 가운데 | `alignment: 1` |
| 자간 / 행간 | 0 / 0.02 | |
| 줄 최대 폭 | 0.82 | `line_max_width: 0.82` |
| **위치** | **가로 중앙, 세로 −0.206** | `transform: {x: 0, y: -0.206}` |
| 스케일 | 1.0 | |

세로 −0.206은 화면 중앙에서 아래로 약 20% 내린 지점이다. 쇼츠 UI가 먹는
하단 18%보다는 위에 있다.

### 애니메이션

| 항목 | 값 |
|---|---|
| 입장 | **"하나씩"** |
| 지속 | **33ms** (111개 중 100개. 나머지는 500ms·36ms) |
| 퇴장 | **없음** |

33ms는 사실상 즉시 나타나는 값이다. 글자가 또르륵 흐르는 연출이 아니라
**딱딱 끊어 찍히는** 느낌이라는 뜻이다. 프리셋으로 저장해 두고 매번 불러 쓴다.

### ★ 자막 분할 — 한 줄 · 어절 보존

실측 결과 **111개 전부 한 줄이고 줄바꿈이 0개**다. 3~17자, 중앙값 9자.

이 규칙을 손으로 지키기 어려우므로 도구로 강제한다.

```bash
python tools/subtitle_split.py "산출물/EP01_진시황릉/01.대본.txt" --srt
```

| 규칙 | 값 | 근거 |
|---|---|---|
| 한 줄 | 줄바꿈 금지 | 실측 111/111 |
| 상한 | **16자** | 실측 최대 17자에서 1자 여유 |
| 목표 | 9자 안팎 | 실측 중앙값 |
| 경계 | **어절(공백)에서만** | |
| 숫자+단위 분리 금지 | `2천 년을` | "2천 / 년을" 방지 |
| 보조용언 분리 금지 | `갈아 넣었죠` | "갈아 / 넣었죠" 방지 |
| 조사·의존명사 단독 시작 금지 | | "것/겁/수/때…"가 줄 앞에 홀로 오지 않게 |
| 짧은 꼬리 재분배 | 3자 미만 | |

출력은 `자막.json`(큐 목록)과 `--srt` 지정 시 `자막.srt`다.
**SRT의 타이밍은 임시값이다.** 실제 싱크는 캡컷에서 TTS에 맞춘다.
상한을 넘는 큐가 하나라도 나오면 종료 코드 1로 알려준다 — 그때는 배속이 아니라
**대본 문장을 줄인다.**

### 타이밍 (실측)

| 항목 | 값 |
|---|---|
| 자막 1개 길이 | 0.30 ~ 2.17초 (중앙 1.07초) |
| 표시 속도 | 5.3 ~ 13.3자/초 (중앙 8.5) |
| 자막 사이 간격 | 0 ~ 0.30초 (중앙 **0.017초**) |

간격 중앙값이 0.017초라는 건 **자막이 거의 끊김 없이 이어진다**는 뜻이다.
빈 화면을 만들지 않는다.

---

## 5-4. 워터마크 (실측)

| 항목 | 값 |
|---|---|
| 소재 | PNG 1254×1254 |
| 위치 | **x = 0.6772, y = −0.8164** (우하단) |
| 스케일 | **0.2304** |
| 불투명도 | 1.0 |
| 크로마키 | **`#fcfcfc`** — 흰 배경을 빼서 로고만 남김 |
| 구간 | 영상 전체 (0.17초 ~ 끝) |
| 트랙 | 별도 오버레이 트랙 (`flag: 2`) |

> ⚠️ 현재 워터마크 원본이 `C:/Users/7500F/Downloads/exec-….png` 에 있다.
> 다운로드 폴더는 지워지기 쉬우니 **채널 폴더로 옮기고 캡컷에서 다시 연결**할 것.
> 권장 위치: `자산/워터마크.png`

---

## 5-4b. 온스크린 라벨 (4단계에서 뺀 것)

이미지 생성 때 글자가 뭉개져 뺀 영문 기술 라벨을 여기서 얹는다.

| 항목 | 값 |
|---|---|
| 폰트 | 영문 산세리프 (Archivo / Helvetica 계열) |
| 색 | 흰색 또는 밝은 적색 |
| 크기 | 자막의 60% |
| 위치 | 해당 부품 옆. 얇은 지시선을 함께 |
| 개수 | 컷당 최대 2개 |

단면·분해·원리 컷(L3~L5)에만. 전경 컷에는 넣지 않는다.

---

## 5-5. 사운드

| 트랙 | 레벨 | 비고 |
|---|---|---|
| 나레이션 | −16 LUFS | 기준 |
| BGM | −26 LUFS | 챕터 전환에서만 살짝 올린다 |
| 효과음 | −22 LUFS | 아래 지점에만 |

효과음은 **아껴야 한다.** 넣는 지점은 셋뿐이다.

1. Ch3 붕괴 순간 — 낮은 임팩트 1회
2. Ch4 단면 진입 순간 — 짧은 스위시 1회
3. Ch4 메커니즘 작동 — 재질에 맞는 소리 1회 (모래/물/금속)

---

## 5-6. 내보내기

| 항목 | 값 |
|---|---|
| 해상도 | **1080×1920 (9:16)** |
| 프레임 | 30fps |
| 비트레이트 | 권장 이상 |
| 코덱 | H.264 |
| 파일명 | `EP01_진시황릉_final.mp4` |
| 저장 위치 | `산출물/EP01_진시황릉/` |

### 내보내기 전 확인

- [ ] 총 길이가 **3분 미만**인가 (쇼츠 상한)
- [ ] 하단 18%에 자막·중요 정보가 걸치지 않는가 (쇼츠 UI가 가린다)
- [ ] 첫 3초에 후킹이 완결되는가
- [ ] 마지막 프레임이 검게 끝나지 않는가
- [ ] 오디오가 끊기거나 겹치는 구간이 없는가
- [ ] 속도표의 **위험** 장면을 전부 처리했는가

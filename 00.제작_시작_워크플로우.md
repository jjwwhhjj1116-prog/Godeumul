# 제작 시작 워크플로 — 고대유물의 비밀

사용자가 “제작 시작”, “다음 주제 제작”, “계속 진행”이라고 말하면 이 문서부터 읽는다.
채팅의 기억보다 GitHub 저장소와 에피소드 잠금 파일을 우선한다.

## A. 저장소와 회차 상태 확인

1. `git status --short`, 현재 브랜치, `HEAD`, `origin/main`을 확인한다.
2. 원격이 앞서 있으면 사용자 변경을 보존한 채 안전하게 동기화한다. 더러운 작업 트리를
   강제로 되돌리거나 덮어쓰지 않는다.
3. `00.함정목록.md`, `채널설정.json`, 이번 단계의 지침을 읽는다.
4. 해당 에피소드의 `00.*`, `01.*`, `02a.*`, 검수·잠금 파일을 열어 마지막 PASS를 찾는다.
5. 파일이 없거나 해시가 다르면 해당 단계부터 다시 수행한다.

## B. 고정 제작 순서

| 순서 | 단계 | 다음 단계로 넘어가는 조건 |
|---:|---|---|
| 0 | 주제 조사·승인 | `00.주제카드.json`, 조사노트, 팩트체크 통과 |
| 1 | 대본 작성 | 사용자 승인된 `01.대본.txt` |
| 1Q | 한국어 3층·문맥 QA | `01.문맥검수.json` v3 전수 검문 PASS + 대본 SHA 일치 |
| 2a | 의미 단위 장면 분할 | 장면 수를 고정하지 않고 문장·행동·증거 전환으로 분할 |
| 3 | TTS 실측 | 문맥 게이트 통과 후 장면 MP3·길이표·발음 검수 |
| 2c | 실물 형태 소유자 잠금 | 박물관·발굴보고서 실사/도면, 고유 지문, 금지 변형, 파일 SHA-256 |
| 2d | 장면별 하이브리드 라우팅 | 모든 장면의 `generation_mode`·`artifact_visibility`·`routing_reason`·참조 ID 확정 |
| 2b/2v | 이미지·영상 프롬프트 | 실측 TTS 비트·고증 카드·카메라·월드 통합 3D 모션 잠금 + I2V/T2V 라우팅 |
| 2G | Flow 투입 기계 게이트 | `prompt_check.py`와 `build_hybrid_flow_pack.py` PASS, 입력 해시 잠금 |
| 4 | Flow/Veo 생성 | 식별 유물은 실사 형태 잠금 I2V, 유물 없는 맥락만 T2V, 시작·중간·끝 키프레임 QA |
| 5 | CapCut 최종 마감 | 템플릿 자막·싱크·효과·오디오를 GUI에서 확인하고 내보내기 |
| 6 | 제목·설명·썸네일 | 대중어 우선 제목, 1080×1920 썸네일 검수 |
| 7 | 업로드 | 비공개 업로드·처리/저작권/썸네일 확인 뒤 다음날 16:00 KST 예약, CapCut 마감 잠금과 업로드 잠금이 현재 파일 해시와 일치 |

전체 단계 순서는 `0 → 1 → 1Q → 2a → 3 → 2c → 2d → 2b/2v → 2G → 4 → 4Q → 5 → 6 → 7`이다.
이 순서는 사용자 승인 없이 바꾸거나 건너뛰지 않는다. TTS 실측 전에 영상 길이를
추측하지 않고, CapCut을 생략한 렌더를 게시 마스터라고 부르지 않는다.

Flow를 열기 직전에는 반드시 `04.하이브리드생성계획.json` version 3의 `gate: PASS`와
`source_hashes`를 `python tools/build_hybrid_flow_pack.py EP경로 --verify`로 현재 파일과
대조한다. 하나라도 다르면 2G부터 다시 실행한다.

식별 가능한 주 유물이 한 장면이라도 있으면 2c 다음에 2e를 실행한다. 공식 실물 사진을
형태 소유자로 삼아 생김새가 동일한 디오라마 기준 이미지를 만든 뒤
`02e.FLOW유물참조잠금.json`에 정확한 한글 유물명, 기준 이미지 SHA-256, Flow 자산명과
`@유물명` 토큰을 잠근다. Flow 제출 직전에는 장면마다 실제 자산 칩이 붙었는지 확인하고
`04.FLOW참조첨부기록.json`에 프로젝트 ID와 첨부 사실을 기록한다. 유물명을 텍스트로만 쓴
프롬프트는 참조 첨부로 인정하지 않는다.

대본 1,400자는 기획 권장선이다. 사용자가 정보 흐름을 승인했고 대본이 1,700자 이하라면
예외 생성할 수 있지만, `durations.json.total_duration`이 180초 이상이면 즉시 대본 단계로
돌아간다. 승인된 내용을 임의로 빼거나 TTS 속도를 올려 상한을 맞추지 않는다.

## C. 대본 한국어·문맥 QA — TTS 전 필수

초안을 쓴 뒤 `korean-proofreader` 스킬을 읽고 문서→문단→문장 순서로 교정한다.
맞춤법 검사만 돌리거나 `~습니다` 반복만 센 것을 한국어 교정 완료라고 부르지 않는다.
그 뒤 다음 검문을 `01.문맥검수.json` **version 3**에 기록한다.

1. 초안을 한 번 저장한 뒤 최근 승인·잠금 대본 1편 이상을 실제로 다시 읽는다. 기본 벤치마크는
   `EP05_로제타석/01.대본.txt`와 `EP04_산싱두이/01.대본.txt`다.
2. `predecessor_flow_review`에 참조 대본 경로·전개 요약·이번 회차에 유지할 원리·복사하지
   않을 소재/표현을 기록한다. 현재 초안이 더 약해진 지점을 찾으면 구조부터 재작성한다.
3. `chapter_flow_review`에서 고정 6챕터 각각의 역할·필수 내용·다음 장 연결을 전수 대조한다.
4. 도입 약속·질문 사다리·기승전결·결론 회수를 문서 층위에서 확인한다.
5. 각 문단의 역할과 전체 정보 흐름을 한 문장으로 요약한다.
6. **모든 인접 문장 쌍**을 시간 순서·인과·질문-답·증거-해석·대조·점층·회수 중
   무엇으로 잇는지 `sentence_links`에 기록한다. `adjacent_sentence_relations: PASS`라는
   선언만으로는 통과하지 않는다.
7. 각 챕터 마지막 질문을 다음 챕터 첫 문장이 직접 받는지 확인한다. 우회한다면 그 이유가
   다음 첫 문장에 있어야 한다.
8. 훅은 5문장 안에 시각적 행동·규모 또는 모순·중심 대상·다음 질문을 모두 담고 군더더기를
   제거한다.
9. 주요 사실 인벤토리를 만든 뒤 각 사실이 `예고→공개→의미 확대→다음 의문`으로 전달되는지
   `reveal_blocks`에 기록한다. `필사본이 나왔습니다` 같은 평면 공개는 재작성한다.
10. 앞에서 설명하지 않은 정보를 뒤 문장의 전제로 쓰지 않는지, `이것·그것·그다음·그 답`의
   선행 대상이 하나로 확정되는지 확인한다.
11. `그런데·하지만·그래서·그러니까` 등 접속어를 지운 판과 비교하고, 남길 이유가
   분명한 경우만 유지한다.
12. 이미 소개한 인물·지위·유물·질문을 새 정보 없이 반복하지 않는지 확인한다.
13. `investigation_review`에 `발견된 이상점→조사 목표→방법/증거→초기 가설→막힌 이유→
    질문 전환→새 정보→확인된 진전→미해결 핵심/이유→미래 증거→역사적 기대효과`를
    기록한다. 단서가 조사 목표나 가설과 연결되지 않으면 PASS하지 않는다.
14. 결말이 `도입 질문의 확인된 답 → 역사적 의미 → 미해결 핵심과 이유 → 미래에 필요한
    증거와 풀렸을 때의 역사적 가치 → 고정 마무리`로 회수되는지 대조한다.
15. 대본과 편집 설명을 분리한다. `이렇게 가면`, `수정 이유`, `충격이 커집니다` 같은 문장은
    TTS 원문에 넣지 않는다.
16. 모든 지적을 해결하고 현재 대본 SHA-256으로 검수 파일을 잠근다.

실행:

```bash
python tools/script_tone_check.py "산출물/EPXX_주제/01.대본.txt"
python tools/script_context_gate.py "산출물/EPXX_주제/01.대본.txt"
```

둘 중 하나라도 실패하면 `tts_generate.py --run`은 실행할 수 없다. 도구도 같은 검문을
내장하므로 문서만 읽지 않고 코드로 차단한다.

## D. CapCut 최종 마감

이 단계에 들어오면 `skills/ancient-artifact-capcut-finish/SKILL.md`를 먼저 읽고 그대로 수행한다.

1. `subtitle_split → 의미 단위 자막 QA → align_subtitles → capcut_build --check → capcut_build`
   순서를 지킨다. `이·그·저·어느·어떤·무슨·한` 같은 지시·관형어는 큐 끝에 두지 않고
   반드시 다음 명사구와 묶는다.
   `자막_싱크.json`이 없거나 1큐라도 검증에 실패하면 빌더가 중단되어야 하며,
   글자수 비율 근사 자막을 게시용 드래프트에 쓰지 않는다.
2. 검증된 `투탕카멘_고대유물의 비밀` 템플릿에서 자막 스타일과 워터마크를 상속한다.
3. CapCut에서 실제 프로젝트를 열어 KCC간판체, 크기 12, 검정 획 0.08, 위치 −0.206,
   한 줄 자막, `페이드 인` 0.25초, 실측 싱크를 눈으로 확인한다.
4. Veo가 공간·단면·분해·흐름 같은 3D 설명모션을 맡는다. CapCut 영상 애니메이션은
   `줌 1`만, 장면 전환은 `왼쪽으로 밀기`만 쓴다. 효과 씹힘 방지를 위해 모든 장면의
   `줌 1` 적용을 먼저 끝낸 뒤 `왼쪽으로 밀기` 전환을 마지막에 적용한다.
5. 모든 영상 원음과 TTS에는 **음량 노멀라이제이션만** 적용한다. 음성 보정·노이즈 제거·
   보컬 분리는 끈다. 영상 원음은 −15dB, TTS는 +5dB, 목표는 −23 LUFS이며
   `capcut_audio_guard.py` 통과 뒤에만 내보낸다.
6. 처음부터 끝까지 실제 재생해 TTS·자막 누적 드리프트와 씹힌 발음을 듣는다. 문제가
   있으면 해당 장면을 문장 단위 TTS로 복구하고 **전체 강제 정렬부터** 다시 수행한다.
7. 2160×3840, H.264 MP4, 30fps로 내보낸 뒤 `capcut_final_lock.py`로 파일 해시와
   시각·오디오 검수 결과를 잠근다. 긴 OneDrive 경로로 직접 내보내기가 실패하면 짧은
   로컬 경로에 먼저 내보내고 실제 파일·메타데이터를 확인한 뒤 결과 폴더로 복사한다.

Remotion은 작업 프리뷰, 자막 싱크 확인, 비상 복구에 쓸 수 있다. CapCut 최종 마감과
`05.캡컷마감잠금.json`을 대신하지 않는다.

## E. YouTube 공개 예약 고정

1. 게시 마스터는 항상 **비공개**로 먼저 업로드한다. 즉시 공개는 금지한다.
2. HD/4K 처리, 제한·저작권, 제목·설명, 9:16 맞춤 썸네일을 실제 업로드 결과에서 확인한다.
3. 이상이 없을 때 `youtube_status_update.py VIDEO_ID --schedule --run`으로 예약한다.
4. 예약 시각은 예약 실행일 기준 **다음날 오후 4시(16:00), Asia/Seoul**로 자동 계산한다.
5. 다른 시각의 명시 예약과 `--공개 공개`는 `채널설정.json`의 게이트가 차단한다.
6. 사용자가 특정 시각을 직접 지시한 경우에만 `--예약정책예외`를 붙여 그 시각으로 예약한다.
7. 영상 ID 생성 직후 업로드 체크포인트를 남기고, 썸네일 등록 응답과 원격 조회가 둘 다 확인돼야 업로드 완료로 기록한다.
